### 🛠 Универсальный подход к тестированию дисков: правильные однострочники для SSD и HDD

Для корректного сравнения производительности важно использовать **честные методы**, исключающие чтение из кеша ядра (`page cache`). Ниже — отдельные однострочники для разных типов накопителей.

---

#### ✅ Тест для **SSD (внутренних и внешних)** — с контролем кеша, но с учётом реальной работы

Используйте этот вариант, если хотите оценить **максимальную пропускную способность через кеш** (реалистичный сценарий для системного диска).

```bash
cd /путь/к/диску && rm -f testfile && \
dd if=/dev/zero of=testfile bs=1M count=2048 conv=fdatasync status=progress && \
sudo sh -c 'echo 3 > /proc/sys/vm/drop_caches' && \
sleep 2 && \
fio --name=read-ssd --filename=testfile --rw=read --bs=1M --size=2G \
    --direct=0 --iodepth=16 --runtime=30 --time_based --ramp_time=5 \
    --output=read-result.txt && \
fio --name=write-ssd --filename=testfile --rw=write --bs=1M --size=2G \
    --direct=0 --sync=0 --iodepth=16 --runtime=30 --time_based --ramp_time=5 \
    --output=write-result.txt && \
echo "Результаты:" && grep "bw=" write-result.txt read-result.txt
```

> 🔎 Подходит для NVMe, SATA SSD, внешних SSD.  
> `drop_caches` перед чтением гарантирует, что первый прогон будет с диска.

---

#### ✅ Тест для **HDD (и медленных USB-накопителей)** — честное чтение без циклов

Для HDD **нельзя использовать `--time_based`**, иначе fio начнёт читать файл повторно — и второй проход пойдёт из RAM. Используйте однократное чтение.

```bash
cd /путь/к/диску && rm -f testfile && \
dd if=/dev/zero of=testfile bs=1M count=2048 conv=fdatasync status=progress && \
sync && \
sudo sh -c 'echo 3 > /proc/sys/vm/drop_caches' && \
sleep 2 && \
fio --name=read-hdd --filename=testfile --rw=read --bs=1M --size=2G \
    --direct=0 --iodepth=1 --numjobs=1 --loops=1 --time_based=0 \
    --output=read-result.txt && \
fio --name=write-hdd --filename=testfile --rw=write --bs=1M --size=2G \
    --direct=0 --sync=0 --iodepth=16 --runtime=30 --time_based --ramp_time=5 \
    --output=write-result.txt && \
echo "Результаты:" && grep "bw=" write-result.txt read-result.txt
```

> 🔎 Ключево: `--loops=1 --time_based=0` → чтение ровно один раз, без повторов.  
> Это исключает влияние page cache после первого цикла.

---

### 📊 Сравнение скорости хранилищ (последовательное чтение/запись)

| Устройство / Подключение | Чтение (MiB/s) | Запись (MiB/s) | Интерфейс | Комментарий |
|--------------------------|----------------|----------------|-----------|-----------|
| 🔵 Внутренний NVMe SSD | 4 607 | 4 086 | PCIe 4.0 x4 | Эталонная скорость, современный NVMe |
| 🖧 Внешний SSD («толстый» провод) | 388 | 394 | USB 3.x (Gen 1/2) | Типично для быстрого внешнего SSD |
| ⚫ Внешний SSD («тонкий» провод) | 36.8 | 21.6 | USB 2.0 | Очень медленно, ограничение кабеля/порта |
| 🔴 Внешний SSD (красный кабель) | 36.9 | 22.6 | USB 2.0 | Аналогичен «тонкому» — работает на USB 2.0 |
| 🟨 WD Elements (HDD, внешний) | **109** | **111** | USB 3.0+ | Реальные скорости: запись хорошая, чтение типичное |

---

### 💡 Дополнительно: отзывчивость и IOPS

Помимо пропускной способности, **отзывчивость системы** зависит от:
- **Случайных операций (4K random read/write)** — измеряются в IOPS.
- **Задержки (latency)** при доступе к мелким файлам.

#### Оценка по отзывчивости:

| Устройство | 4K Random Read (ожидаемо) | 4K Random Write | Отзывчивость в реальной работе |
|----------|----------------------------|------------------|-------------------------------|
| 🔵 Внутренний NVMe SSD | 500k–1M IOPS | 400k–800k IOPS | Мгновенная загрузка ОС, приложений, браузер без лагов |
| 🖧 Внешний SSD (USB 3.x) | 10k–40k IOPS | 8k–30k IOPS | Хорошо для переноса данных, но заметны задержки в файлах |
| ⚫ / 🔴 Внешние SSD (USB 2.0) | < 1k IOPS | < 500 IOPS | Очень высокие задержки, "подвисает" при работе с множеством файлов |
| 🟨 WD Elements (HDD) | 0.5–1.0 kIOPS | 0.3–0.8 kIOPS | Высокие задержки, долгая индексация, тормозит при многозадачности |

> 🔍 Примечание: HDD физически ограничен из-за механики — головке нужно двигаться по дискам, поэтому **случайный доступ в 100–1000× медленнее**, чем у NVMe.

---

### ✅ Общий вывод

- **Внутренний NVMe SSD** — лидер по **скорости и отзывчивости**. Идеален для ОС, рабочих задач, виртуализации.
- **Внешний SSD по USB 3.x** — хороший выбор для **резервного копирования и мобильности**.
- **USB 2.0 (любой кабель)** — слишком медленный для SSD; подходит только для простых задач.
- **WD Elements (HDD)** — надёжное хранилище большого объёма, но **не для системного диска или активной работы**.

💡 **Совет**: используйте NVMe для системы и приложений, HDD — для архивов, внешние SSD по USB 3+ — для переноса данных. Избегайте USB 2.0 для любых дисков, где важна скорость.
