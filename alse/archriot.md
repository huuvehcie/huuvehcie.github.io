<div align="center">

# :: 𝔸𝕣𝕔𝕙ℝ𝕚𝕠𝕥 ::

![Version](https://img.shields.io/badge/version-4.2.2-blue?labelColor=0052cc)
![License](https://img.shields.io/github/license/CyphrRiot/ArchRiot?color=4338ca&labelColor=3730a3)
![Platform](https://img.shields.io/badge/platform-linux-4338ca?logo=linux&logoColor=white&labelColor=3730a3)
![Arch Linux](https://img.shields.io/badge/Arch_Linux-1e1b4b?logo=arch-linux&logoColor=8b5cf6&labelColor=0f172a)
![Wayland](https://img.shields.io/badge/Wayland-312e81?logo=wayland&logoColor=a855f7&labelColor=1e1b4b)

![Last Commit](https://img.shields.io/github/last-commit/CyphrRiot/ArchRiot?color=5b21b6&labelColor=4c1d95)
![Code Size](https://img.shields.io/github/languages/code-size/CyphrRiot/ArchRiot?color=4338ca&labelColor=3730a3)
![Code](https://img.shields.io/badge/human-coded-blue?logo=data:image/svg+xml;base64,&logoColor=a855f7&labelColor=1e1b4b)

![Language](https://img.shields.io/badge/language-Go-4338ca?logo=go&logoColor=c7d2fe&labelColor=3730a3)
![Language](https://img.shields.io/badge/language-YAML-5b21b6?logo=yaml&logoColor=e0e7ff&labelColor=4c1d95)
![Language](https://img.shields.io/badge/Python-312e81?label=language&logo=python&logoColor=c7d2fe&labelColor=1e1b4b&color=312e81)

</div>

## **ArchRiot: Система (Arch) Linux, о которой вы мечтали (но никогда не имели)**

### **Одна команда. Полное окружение. Никаких компромиссов.**

ArchRiot — это ответ на каждый раз, когда вы думали: «почему Linux просто не может работать правильно с самого начала?». Мы потратили сотни часов на совершенствование деталей, чтобы вы получили молниеносно быструю, безопасную, красивую систему, которая действительно уважает ваше время и интеллект.

**Тщательно настроенный для правильной работы:**

- **🪟 Тайлинг Hyprland** — Заставляет другие оконные менеджеры казаться примитивными
- **⚡ Установщик на Go** — Атомарные операции, мгновенный откат, никакого адского зависимостей
- **🛡️ Конфиденциальность** — Нулевая телеметрия, нулевой отслеживание, нулевой сбор корпоративных данных
- **🎨 Эстетика** — Тщательно созданные темные темы, которые работают в любое время суток
- **💻 Разработка** — Zed, Neovim, улучшения оболочки и другие апгрейды

_Создан на базе Arch Linux с Hyprland, потому что компромиссы — для других людей. Это не поддерживается комитетом или корпоративной дорожной картой — это поддерживается кем-то с одержимой, единственной целью — сделать это правильно с первого раза, потому что плохие окружения Linux — это оскорбление того, чем должны быть вычисления._

![ArchRiot Screenshot](config/images/screenshot.png)

<iframe width="560" height="315" src="https://www.youtube.com/embed/qrraIOvAcdg?si=yooSbgD0FJKLuAkc" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>

## 📚 Навигация по этому руководству

- [🚀 Выберите свой опыт ArchRiot](#choose-your-archriot-experience)
    - [🔥 Метод 1: Скрипт установки](#method-1-install-script)
    - [⚡ Метод 2: ArchRiot ISO](#method-2-archriot-iso)
        - [Установка ISO: Исправление прерывания Wi‑Fi (iwd PSKs)](#iso-wifi-abort)

- [⌨️ Овладите рабочим столом ArchRiot](#master-your-archriot-desktop)
- [🎛️ Панель управления](#archriot-control-panel)
- [🎯 Ключевые настройки](#key-customizations)
- [🔄 Управление системой](#system-management)
- [🧰 Расширенное использование: флаги CLI](#advanced-usage-archriot-cli-flags)
- [🔧 Устранение неполадок](#troubleshooting)
- [📄 Лицензия](#license)

## ⚡ Быстрая установка

- 🔥 Метод 1 — Скрипт установки (для существующего Arch):

    ```bash
    curl -fsSL https://ArchRiot.org/setup.sh | bash
    ```

    См.: [🔥 Метод 1: Скрипт установки](#method-1-install-script)

- ⚡ Метод 2 — ArchRiot ISO (чистая установка):
    - Скачайте из Releases и загрузитесь с ISO:
      https://github.com/CyphrRiot/ArchRiot/releases
    - См.: [⚡ Метод 2 — ArchRiot ISO](#method-2-archriot-iso)

<a id="choose-your-archriot-experience"></a>

## 🚀 Выберите свой опыт ArchRiot

<a id="method-1-install-script"></a>

### 🔥 Метод 1: Скрипт установки

#### У вас уже установлен Arch Linux

**Превратите вашу текущую систему Arch в ArchRiot**

```bash
curl -fsSL https://ArchRiot.org/setup.sh | bash
```

**Идеально подходит для:**

- 🏠 **Сохранения системы** — Сохраните свои данные, конфиги и персональные настройки в целости
- 🔧 **Вариантов Arch** — CachyOS, Manjaro, EndeavourOS или чистых установок Arch
- 🎨 **Апгрейда рабочего стола** — Измените только ваше окружение рабочего стола, оставив все остальное как есть
- ⚡ **Быстрых побед** — Получите лучшие функции ArchRiot без начала с нуля

**Что вы получаете:**

- Окружение рабочего стола и приложения ArchRiot
- Тайлинговый оконный менеджер Hyprland
- Темы CypherRiot и настройки
- Ваша существующая система остается нетронутой

#### **Примечание:** Для новых установок у нас также есть доступен **ArchRiot ISO** (см. ниже «Метод 2»)

<a id="method-2-archriot-iso"></a>

### ⚡ Метод 2: ArchRiot ISO

#### У вас НЕ установлен Arch Linux

![ArchRiot Installation Demo](config/images/riot.gif)

#### ⚠️ Предупреждение: ISO заменит диск на ArchRiot Linux. ⚠️

1. **📥 Скачайте ISO ArchRiot Linux**
    - **[ArchRiot Linux ISO](https://github.com/CyphrRiot/ArchRiot/releases/download/v2.27/archriot.iso)**
    - **[Контрольная сумма SHA256](https://github.com/CyphrRiot/ArchRiot/releases/download/v2.27/archriot.sha256)**

Примечание: _Не используйте wget_ для этого. Curl — ваш друг:

```bash
curl -L -o archriot.iso https://github.com/CyphrRiot/ArchRiot/releases/download/v2.27/archriot.iso
```

2. **💾 Скачайте Ventoy**
    - Скачайте [Ventoy](https://www.ventoy.net/) и создайте загрузочную USB-флешку
    - _Совет профи: Ventoy позволяет загружаться с нескольких ISO с одной флешки — идеально для тестирования_

3. **📂 Скопируйте ISO на диск Ventoy**
    - Просто скопируйте файл ArchRiot ISO прямо на вашу флешку Ventoy
    - Прошивка не нужна — просто скопируйте файл

4. **⚡ Загрузитесь с ISO ArchRiot**
    - Загрузитесь с USB (отключите Secure Boot, если ваш BIOS капризничает)
    - Выберите ArchRiot ISO в меню Ventoy

_Примечание: В некоторых странных, редких случаях dhcpd не запущен. В этом случае выполните:_

`sudo systemctl start dhcpcd`

Затем выполните `curl -fsSL https://ArchRiot.org/setup.sh | bash`, чтобы продолжить последний этап установки.

Примечание: Если есть проблема с Wi-Fi (после первой загрузки), см.: [Установка ISO: Исправление прерывания Wi‑Fi (iwd PSKs)](#iso-wifi-abort)

```bash
iwctl
device list
station {device} scan
station {device} get-networks
station {device} connect {network}
station {device} show
exit
```

**Идеально подходит для:**

- 🖥️ **Свежего железа** — Новые сборки, чистый лист, виртуальные машины
- 🚀 **Мгновенного удовлетворения** — Загрузка → `riot` → Идеальный рабочий стол за минуты
- 💀 **Замены системы** — Когда ваша текущая установка разочаровывала вас в последний раз
- 🎯 **Нулевой конфигурации** — Для людей, у которых есть дела поважнее, чем настройка конфигов

**Что вы получаете:**

- Полная система Arch Linux + ArchRiot
- Загрузка с ISO → Запуск `riot` → Полная руководимая установка
- Hyprland, темы, приложения — все предварительно настроено
- Ручная настройка не требуется

<a id="iso-wifi-abort"></a>

### Устранение неполадок (Установка ISO): Прерывание Wi‑Fi при «Wi‑Fi configuration/services not properly set on target»

Причина

- Live ISO использует iwd/iwctl, в то время как передача первого запуска установщика ранее ожидала соединения NetworkManager (NM) на целевой системе. Если на целевой системе не настроено пригодное для использования соединение, установщик прерывает работу, чтобы избежать перезагрузки без сети.

Исправление без изменения ISO
Выполните это из приглашения archiso после ошибки (корень целевой системы смонтирован в /mnt):

1. Предпочтительный: скопируйте PSK iwd и включите iwd + DHCP (без NetworkManager)

```bash
# Enable iwd on target; avoid conflicts
arch-chroot /mnt systemctl enable iwd
arch-chroot /mnt systemctl mask wpa_supplicant

# Ensure DHCP will bring up networking at boot (when not using NM)
arch-chroot /mnt pacman -Sy --noconfirm --needed dhcpcd
arch-chroot /mnt systemctl enable dhcpcd

# Copy the live iwd credentials (PSKs) to the target
arch-chroot /mnt mkdir -p /var/lib/iwd
cp -a /var/lib/iwd/*.psk /mnt/var/lib/iwd/ 2>/dev/null || true
arch-chroot /mnt chmod 600 /var/lib/iwd/*.psk 2>/dev/null || true
```

2. Необязательная альтернатива: используйте NetworkManager на целевой системе (с бэкендом iwd)

```bash
# Install and enable NetworkManager on target
arch-chroot /mnt pacman -Sy --noconfirm --needed networkmanager iwd
arch-chroot /mnt bash -lc 'mkdir -p /etc/NetworkManager/conf.d &&
  printf "[device]\nwifi.backend=iwd\n" > /etc/NetworkManager/conf.d/10-wifi-backend.conf &&
  printf "[connection]\nwifi.powersave=2\n" > /etc/NetworkManager/conf.d/40-wifi-powersave.conf'
arch-chroot /mnt systemctl enable NetworkManager

# Seed NM connections from iwd (best effort)
for f in /var/lib/iwd/*.psk; do
  [ -e "$f" ] || continue
  ssid="$(basename "$f" .psk)"
  pass="$(awk -F= '/^Passphrase=/{print $2}' "$f")"
  if [ -n "$pass" ]; then
    arch-chroot /mnt nmcli c add type wifi ifname "*" con-name "$ssid" ssid "$ssid"
    arch-chroot /mnt nmcli c modify "$ssid" wifi-sec.key-mgmt wpa-psk wifi-sec.psk "$pass"
  else
    echo "No Passphrase in $f; skipping NM import for $ssid (PSK-only)"
  fi
done
arch-chroot /mnt bash -lc 'chmod 600 /etc/NetworkManager/system-connections/*.nmconnection 2>/dev/null || true'
```

3. Перезагрузитесь и подключитесь

- Путь iwd: система должна автоматически подключиться с использованием PSK; если нет, запустите «iwctl» и подключитесь к своему SSID.
- Путь NM: используйте «nmtui», если автоматическое подключение не происходит.

Быстрое решение

- Используйте проводное (Ethernet) соединение во время установки; контрольная точка безопасности пропустит при обнаружении проводного соединения.

Примечание о Secure Boot

- Это конкретное прерывание Wi‑Fi не вызвано Secure Boot. Некоторые системы все еще требуют отключения Secure Boot по другим причинам; при необходимости проконсультируйтесь с руководством производителя.

---

**Примечание по безопасности:** Ваша система остается защищенной благодаря шифрованию диска LUKS и блокировке экрана. Passwordless sudo является стандартом для автоматизированных установок системы и не нарушает безопасность при правильной настройке шифрования диска.

#### 🚀 Установка или обновление в одну строку

> Предупреждение: При настройках с несколькими мониторами обновление может временно остановить Brave во время согласования дисплеев.
>
> Смягчение последствий:
>
> - Предпочитайте обновление при закрытом Brave.
> - Если Brave затронут, безопасно восстановите работу с помощью:
>     - `~/.local/share/archriot/install/archriot --displays-enforce`
>     - `~/.local/share/archriot/install/archriot --waybar-reload`
> - Дисплеи также применяются повторно автоматически при следующем входе в систему (принуждение при входе).

**Единственная команда, которую нужно запомнить:**

```bash
curl -fsSL https://ArchRiot.org/setup.sh | bash
```

![ArchRiot Upgrade Demo](config/images/upgrade.gif)

Это загружает и запускает наш неубиваемый установщик бинарных файлов на Go с интеллектуальной конфигурацией YAML. Обновление — это точно такая же команда — потому что простота — это высшая форма sophistication.

**Что происходит:** Автоматическая установка пакетов, развертывание конфигурации и настройка системы с полной возможностью отката, если что-то пойдет не так.

<a id="master-your-archriot-desktop"></a>

## ⌨️ Овладение рабочим столом ArchRiot

_Горячие клавиши, которые заставят вас задуматься, как вы вообще пользовались мышью_

### 🎯 Начало работы

_Ваш путь к овладению ArchRiot — запомните их в первую очередь_

| Клавиша             | Действие                                          |
| ------------------- | ------------------------------------------------- |
| `SUPER + H`         | Показать СПРАВКУ — Ваша спасительная линия, когда вы потерялись |
| `SUPER + D`         | Запуск приложений — Найдите что угодно мгновенно |
| `SUPER + RETURN`    | Терминал — Ваш командный центр (Ghostty)          |
| `SUPER + L`         | Блокировка экрана — Красивый стиль CypherRiot     |
| `SUPER + ESCAPE`    | Меню питания — Сон, перезагрузка или выключение  |

### 🪟 Управление окнами (Наиболее используемое)

_Мастерство тайлинга — то, где ArchRiot действительно сияет_

| Клавиша                       | Действие                                                |
| ---------------------------- | ------------------------------------------------------- |
| `SUPER + W` или `SUPER + Q`   | Закрыть окно — Прощай навсегда                          |
| `SUPER + V`                  | Переключить плавающее — Освободитесь                    |
| `SUPER + J`                  | Переключить раздел — Реорганизуйте пространство         |
| `SUPER + Клавиши со стрелками`| Переместить фокус — Навигируйте как профессионал       |
| `SUPER + SHIFT + Клавиши со стрелками`| Поменять окна местами — Идеальное перераспределение |
| `SUPER + CTRL + Клавиши со стрелками| Умное перемещение — Позвольте Hyprland подумать    |
| `SUPER + SHIFT + TAB`        | Исправить окна за пределами экрана — Инструмент ручного спасения |
| `SUPER + 1-4`                | Переключить рабочее пространство — Ваши цифровые комнаты |
| `SUPER + SHIFT + 1-4`        | Переместить окно в рабочее пространство — Переместить  |
| `SUPER + SHIFT + RETURN`     | Плавающий терминал — Когда вам нужна мощность наложения |

### 💻 Основные приложения

_Инструменты, которые имеют значение, в одном нажатии клавиши_

| Клавиша               | Действие                                                    |
| --------------------- | ----------------------------------------------------------- |
| `SUPER + F`           | Файловый менеджер — Навигация по вашему цифровому королевству (Thunar) |
| `SUPER + B`           | Браузер — Веб с упором на конфиденциальность (Brave)        |
| `SUPER + Z`           | Редактор кода — Современная разработка (Zed)                |
| `SUPER + N`           | Текстовый редактор — Рай для опытных пользователей (Neovim) |
| `SUPER + O`           | Простой текстовый редактор — Чистый и быстрый (GNOME)       |
| `SUPER + T`           | Системный монитор — Видите все (btop)                       |
| `SUPER + SHIFT + RETURN` | Плавающий терминал — Режим мощности наложения          |

### 💬 Коммуникация и социальные сети

_Оставайтесь на связи, не продавая свою душу майнерам данных_

| Клавиша  | Действие                                                    |
| ----------- | ----------------------------------------------------------- |
| `SUPER + E` | Электронная почта — Зашифрованная и приватная (Proton Mail) |
| `SUPER + G` | Мессенджер Telegram — Фокус или запуск (нативный/Flatpak)   |
| `SUPER + S` | Мессенджер Signal — Умная интеграция Signal                 |
| `SUPER + M` | Google Messages — Когда вы обязаны использовать машину      |
| `SUPER + K` | Google Keep — Быстрые заметки и списки                       |
| `SUPER + X` | X/Twitter                                                   |

Примечание: `SUPER + SHIFT + K` — Стабилизировать сеанс: перезапускает Waybar, перезапускает hypridle и применяет настройки дисплеев (без полной перезагрузки).

### 📸 Скриншоты и запись

_Запечатлейте величие ArchRiot и поделитесь им со всем миром_

| Клавиша             | Действие                                             |
| ------------------- | ---------------------------------------------------- |
| `SUPER + SHIFT + S` | Скриншот области — Выберите именно то, что важно     |
| `SUPER + SHIFT + W` | Скриншот окна — Идеальный захват приложений         |
| `SUPER + SHIFT + F` | Полный экран — Покажите весь свой рабочий стол       |
| `Kooha`             | Запись экрана — Создавайте учебники (запуск через `SUPER+D`) |

### 🎨 Управление обоями и динамическая тема

_Поддерживайте свежесть вашего рабочего стола с помощью интеллектуальной системы обоев ArchRiot и автоматической смены цветов темы_

| Клавиша             | Действие                                      |
| ------------------ | --------------------------------------------- |
| `SUPER + CTRL + SPACE` | Циклическая смена фона — Свежие вибрации по требованию |

**🌈 Динамическое изменение цвета темы:**

- **Необязательная интеллектуальная тема** — Включите в Панели управления для извлечения цветов из ваших обоев
- **Обновления waybar в реальном времени** — Когда включено, цвета рабочих пространств, индикаторы ЦП и акценты меняются мгновенно в соответствии с обоями
- **Переключатель управления** — Включите/отключите динамическую тему в Панели управления или оставьте классические цвета CypherRiot
- **Умный откат** — Всегда возвращается к красивой теме CypherRiot при отключении
- **⚠️ Примечание об обновлении** — Обновления системы сбросят динамическую тему на настройки CypherRiot по умолчанию (просто снова включите в Панели управления)

**🎛️ Магия панели управления:**

- Запустите Панель управления ArchRiot для управления обоями методом перетаскивания
- Переключатель динамической темы — Включите цвета на основе обоев или придерживайтесь классики CypherRiot
- Системные обои (1-15) + ваша пользовательская коллекция (U1, U2 и т.д.) идеально организованы
- Изменения применяются мгновенно — без перезагрузок, без ожидания, только красота

**🖼️ Сильные ходы с пользовательскими обоями:**

```bash
# Метод профи: используйте выбор файлов в Панели управления
# Быстрый метод: перетащите файлы прямо в ~/.config/archriot/backgrounds/

# Уборка — удалите конкретный обои
rm ~/.config/archriot/backgrounds/user_01.jpg

# Ядерный вариант — начните с чистого листа
rm ~/.config/archriot/backgrounds/*
```

### ⚙️ Управление системой

_Поддерживайте вашу систему ArchRiot работающей как хорошо смазанная машина_

```bash
archriot                             # Интеллектуальный движок установки/обновления ArchRiot
                                     # Автоматически определяет, требуется ли обновление
                                     # Запрашивает подтверждение перед продолжением

migrate                              # Мастер резервного копирования/восстановления — ваша страховка
sudo systemctl reboot                # Свежий старт — иногда он нужен
sudo systemctl poweroff              | Корректное выключение — не крах
```

### 🎵 Аудио и медиа

_Аппаратные мультимедийные клавиши, которые действительно работают — представьте себе!_

```bash
XF86AudioRaiseVolume                 # Громкость вверх — с великолепным оверлеем
XF86AudioLowerVolume                 # Громкость вниз — гладко как шелк
XF86AudioMute                        # Переключить mute — мгновенная обратная связь
XF86AudioMicMute                     # Переключить микрофон — конфиденциальность в одно нажатие
XF86AudioPlay/Pause                  # Управление медиа — работает со всем

# Красивый оверлей громкости появляется мгновенно с индикатором прогресса
# Это ваши реальные аппаратные клавиши, работающие так, как должны
```

### 📊 Элементы управления Waybar (Строка состояния)

_Центр управления вашего рабочего стола — все, что нужно, одним взглядом_

```bash
Нажмите на таймер помидора             # Режим фокуса Pomodoro — оставайтесь продуктивным
Двойной клик по таймеру помидора        # Сброс до 25:00 — свежий старт
Нажмите на значок сети                 # Менеджер сети — подключайтесь откуда угодно
Нажмите на значок громкости            | Настройки звука — точная настройка
Нажмите на значок батареи               # Управление питанием — оставайтесь заряженными
```

### Политика крышки/простоя (ноутбуки, в док-станции и без)

- В док-станции (подключен внешний монитор): закрытие крышки не переводит в спящий режим; система не переходит в спящий режим бездействия.
- Без док-станции: закрытие крышки переводит в спящий режим; спящий режим бездействия наступает через 30 минут по умолчанию.

Реализовано через:

- `/etc/systemd/logind.conf.d/10-docked-ignore-lid.conf`:
    - `HandleLidSwitchDocked=ignore`, `HandleLidSwitch=suspend`, `HandleLidSwitchExternalPower=suspend`
- `/etc/systemd/logind.conf.d/20-idle-ignore.conf`:
    - `IdleAction=ignore` (logind не переходит в спящий режим бездействия)
- hypridle:
    - 10 мин: `on-timeout = lock` (надежный триггер hyprlock)
    - 30 мин: `on-timeout = $HOME/.local/share/archriot/install/archriot --suspend-if-undocked || $HOME/.local/bin/suspend-if-undocked.sh` (переходит в спящий режим только без док-станции)
- автозапуск kanshi (профили hotplug) для управления экранами при подключении/отключении док-станции

Примечание: Мы никогда не перезапускаем systemd-logind во время установки/обновления; drop-ins вступают в силу после перезагрузки или ручного перезапуска, который вы выполните позже.

## 📋 Лог эволюции

_Неуклонное движение к совершенству Linux_

**🔥 Текущий релиз:** v4.2.1 — Полировка документации и UX: консолидация обертки Brave, рекомендации по логам/перезагрузке Waybar, заметки о дробном масштабировании, размер Панели управления, непрозрачность Thunar

**🚀 Последние вехи:**

- **v2.7.4:** Исправления пробуждения DPMS и оптимизация системы
- **v2.7.3:** Улучшенная согласованность тем и исправление ошибок
- **v2.7.2:** Критические исправления установки yay + зависимостей
- **v2.7.1:** Исправления проблем с путями к аппаратным модулям
- **v2.7.0:** Неубиваемая обработка ошибок с быстрым отказом — потому что отказ не вариант
- **v2.6.14:** Waybar с 3 состояниями + восстановление безопасности пакетов
- **v2.6.13:** Неубиваемый setup.sh + улучшения UX Панели управления

```bash
# Просмотреть все изменения версий
git log --grep="FIX" --oneline
```

<a id="archriot-control-panel"></a>

## 🎛️ Панель управления ArchRiot

**Командный центр, делающий Linux действительно удобным для пользователя**

Панель управления ArchRiot — это не просто еще одно приложение настроек — это недостающая часть, которая делает расширенные функции Linux доступными для людей. Созданная с использованием современного GTK4, она быстрая, красивая и действительно имеет смысл.

**Запуск:** `SUPER+C` или запустите `archriot-control-panel` откуда угодно

![ArchRiot Control Panel](config/images/control-panel.png)

### 🎛️ **Функции**

- **🍅 Таймер Pomodoro** — Таймер продуктивности, интегрированный в Waybar, с интервалами 5-60 минут
- **💡 Фильтр синего света** — Управление температурой экрана в реальном времени (2500K-5000K) через hyprsunset
- **🛡️ Mullvad VPN** — Управление учетной записью с элементами управления конфиденциальностью и автоподключением
- **🔊 Аудио система** — Безопасные элементы управления отключением звука без нарушения служб
- **📷 Управление камерой** — Разрешения устройств, настройки разрешения и тестирование в режиме реального времени
- **🖥️ Настройки дисплея** — Разрешение и масштабирование монитора с предпросмотром в реальном времени
- **🔋 Управление питанием** — Профили батареи (Экономия энергии, Сбалансированный, Высокая производительность)

### 🛡️ **Конфиденциальность и безопасность**

- **Конфиденциальность аккаунта** — Чувствительная информация скрыта по умолчанию с переключателем показать/скрыть
- **Безопасные элементы управления** — Отключение звука вместо уничтожения служб, разрешения вместо ломки :
- **Предпросмотр в реальном времени** — Изменения системы в реальном времени с опцией «Выйти без сохранения»
- **Образовательный контент** — Диалоги «Узнать больше» с полным объяснением функций

### 🎨 **Техническое совершенство**

_Потому что вы заслуживаете ПО, которое не отстой_

- **Приложение GTK4** — Передовой интерфейс с великолепной темой CypherRiot
- **Интеграция в реальном времени** — Смотрите, как изменения происходят мгновенно — кнопки «Применить» не нужны
- **Неубиваемая сохраняемость** — Ваши настройки переживают перезагрузки, обновления и системный хаос
- **Модульная архитектура** — Создана правильно с первого дня — расширяемая и поддерживаемая

## 💾 **Встроенное резервное копирование и восстановление с Migrate**

**Ваша идеальная установка ArchRiot ценна — защитите ее как цифровое сокровище, которым она является**

ArchRiot автоматически устанавливает и интегрирует **[Migrate](https://github.com/CyphrRiot/Migrate)** — нашу проверенную в бою систему резервного копирования и восстановления. Это не инструмент-второстепенность — это ваша страховка от сбоев оборудования, ошибок пользователя и неизбежных моментов «что я только что удалил?».

_Потому что повторное создание вашей идеальной настройки в течение недель — это особый вид ада._

### 🛡️ **Почему Migrate важен**

- **Полное резервное копирование системы** — Каждый dotfile, каждая настройка, каждая идеальная конфигурация сохранена
- **Интерактивный TUI** — Великолепный интерфейс терминала, который делает сложные операции простыми
- **Восстановление живой системы** — Восстановление без уничтожения вашей системы — продолжайте работать, пока исправляете
- **Миграция между установками** — Клонирование вашей настройки на новые машины за минуты
- **Нулевое обслуживание** — Обновляется автоматически с ArchRiot — на одну вещь меньше, о которой нужно беспокоиться

### 🔥 **Быстрый старт**

```bash
migrate                              # Запуск интерактивного интерфейса резервного копирования/восстановления
```

**Никаких флагов, никакой сложности** — просто запустите `migrate` и используйте интуитивное меню для резервного копирования или восстановления всей вашей настройки ArchRiot за минуты!

[![Migrate Tool](https://img.shields.io/badge/Backup_Tool-Migrate-6B46C1?style=for-the-badge&logo=github&logoColor=white)](https://github.com/CyphrRiot/Migrate)

<a id="key-customizations"></a>

## 🎯 Ключевые настройки

### 🔧 **Изменения основной системы**

- **Терминал:** Ghostty (заменяет Kitty) с 90% прозрачностью и темной темой
- **Браузер:** Brave (заменяет Chromium) с нативной поддержкой Wayland
- **Файловый менеджер:** Thunar (заменяет Nautilus) с полной темной темой
- **Оболочка:** Fish по умолчанию (заменяет Bash) с правильной настройкой PATH
- **Тема:** CypherRiot интегрирована как единая система тем
- **Редактор кода:** Zed (Wayland) + Neovim с правильной интеграцией тем
- **Приложения:** Все основные приложения теперь работают на нативном Wayland (больше никаких проблем с XWayland)
- **Инструмент резервного копирования Migrate:** Комплексное решение CypherRiot для резервного копирования/восстановления системы (встроенное)
- **Оптимизация памяти:** Интеллектуальное управление памятью, которое действительно работает
- **Фильтр синего света:** hyprsunset при 3500K для снижения нагрузки на глаза (настраивается)
- **Темы GTK:** Темная тема везде — никаких режущих глаза белых диалогов
- **Масштабирование DPI:** Исправлены проблемы масштабирования для единообразного интерфейса во всех приложениях

#### 🧠 **Исправление управления памятью**

Управление памятью по умолчанию в Linux **агрессивно глупо** в отношении кеширования. Ядро с радостью съедает 90%+ вашей ОЗУ под файловые кеши, а затем с трудом освобождает ее, когда приложениям действительно нужна память.

**Решение ArchRiot:** Комплексная настройка управления памятью, которая обеспечивает:

**Основные улучшения:**

- **Умное кеширование** — Резервирует 1 ГБ ОЗУ, снижает агрессивное кеширование файловой системы
- **Минимальный своп** — 10% swappiness (против 60% по умолчанию) сохраняет все в ОЗУ
- **Запись без задержек** — Лимит грязных страниц 5% предотвращает массовые всплески записи
- **Фоновая очистка** — 2% фоновой записи для плавной производительности

**Расширенная защита:**

- **Контроль перераспределения памяти** — Предотвращает опасное выделение памяти, вызывающее сбои
- **Проактивная дефрагментация** — Снижает фрагментацию памяти для лучшего выделения
- **Умный OOM Killer** — Убивает проблемные процессы, а не случайные системные службы
- **Повышенная отзывчивость** — Оптимизированные интервалы грязных страниц и пропускная способность памяти

**Реальное влияние:**

- **Больше никаких задержек** при открытии приложений или переключении окон
- **Лучшая отзывчивость** под сильной нагрузкой на память (протестировано при использовании 75%+ ОЗУ)
- **Сниженное использование свопа** при интеллектуальном использовании ОЗУ
- **Стабильность системы** при экстремальных нагрузках — никаких зависаний или сбоев

**Результат:** Ваша система остается быстрой и отзывчивой даже при запуске нескольких приложений, компиляции кода или при экстремальном стресс-тестировании.

### 📱 **Расширенная интеграция Waybar**

ArchRiot включает в себя сильно настроенный Waybar (строку состояния) с комплексной интеграцией системы:

**Встроенные модули:**

- **🍅 Таймер Pomodoro** — Встроенный таймер Pomodoro (бездействие/работа/перерыв/завершено)
- **🛡️ Статус Mullvad VPN** — Статус VPN-соединения в реальном времени с отображением местоположения
- **📊 Мониторинг системы** — Сводное использование ЦП, точный мониторинг памяти
- **📊 Визуальные метрики системы** — Температура, ЦП, память и громкость
    - отображаются как интуитивно понятные индикаторы-полоски (▁ ▂ ▃ ▄ ▅ ▆ ▇ █)
- **🎤 Управление микрофоном** — Визуальный статус микрофона с переключением в один клик
- **📶 Управление сетью** — Статус Wi-Fi с интеграцией nmtui
- **🔊 Управление аудио** — Отображение громкости с интеграцией аппаратных клавиш

**Технические улучшения:**

- **Исправлен парсер CSS** — Устранены все объявления !important, вызывающие ошибки waybar
- **Пользовательские разделители** — Чистый, организованный макет модулей для лучшей читаемости
- **Система прозрачности** — Единообразная прозрачность 90-98% во всех приложениях
- **Оптимизация шрифтов** — Улучшен формат даты (Воскресенье • 13 июля • 01:49 PM)
- **Работа без ошибок** — Все модули проверены и протестированы на надежность

### 📱 **Чистые веб-приложения**

- **Proton Mail** (SUPER+E / XF86Mail) — Электронная почта с упором на конфиденциальность в плавающем окне
- **Telegram** (SUPER+G) — Приложение для приватных сообщений
- **Signal** (SUPER+S) — Приложение для приватных сообщений
- **Google Messages** (SUPER+M) — Веб-сообщения в плавающем окне
- **X/Twitter** (SUPER+X) — Социальная платформа в плавающем окне
- **GitHub** — Платформа для разработки с правильными иконками от homarr-labs

### 🪟 **Адаптивное управление окнами**

- **Масштабирование в процентах** — Окна правильно масштабируются на разных разрешениях экрана (1080p, 1440p, 4K, ультраширокие)
- **Умное центрирование** — Все плавающие окна автоматически центрируются независимо от размера монитора
- **Совместимость с разными разрешениями** — Нет жестко заданных позиций в пикселях, работает на любой конфигурации дисплея
- **Оптимизированные окна приложений:**
    - **X/Twitter**: `40% x 90%` (макет в мобильном стиле с адаптивной высотой)
    - **Proton Mail**: `45% x 80%` (идеальные размеры для чтения почты)
    - **Google Messages**: `40% x 85%` (удобный интерфейс сообщений)
    - **Signal**: `40% x 80%` (сохраняет нативный опыт рабочего стола)
- **Дизайн, устойчивый к будущему** — Правила окон автоматически адаптируются к новым конфигурациям мониторов

### 🎮 **Поддержка GPU**

ArchRiot автоматически обнаруживает и устанавливает оптимальные драйверы для всех основных GPU:

- **NVIDIA**: Проприетарные драйверы с поддержкой Wayland и аппаратным ускорением
- **AMD/Radeon**: Драйверы с открытым исходным кодом Mesa с поддержкой Vulkan
- **Intel**: Драйверы Mesa, включая поддержку Intel Arc

Все GPU получают правильную интеграцию Wayland и аппаратное ускорение видео для оптимальной производительности.

**Функции производительности:**

- **Аппаратное ускорение** — Воспроизведение видео, эффекты композитинга и рендеринг приложений
- **Нативная поддержка Wayland** — Никаких проблем с совместимостью XWayland
- **Автоматический выбор драйверов** — Ручная настройка не требуется
- **Поддержка Vulkan** — Современный графический API для игр и разработки
- **Оптимизация для нескольких мониторов** — Правильное масштабирование и управление дисплеями

### ⌨️ **Улучшенные горячие клавиши и продуктивность**

- **SUPER+D** = **SUPER+SPACE** (Единый запуск приложений)
- **Левый клик по иконке Arch** — Сетка приложений nwg-drawer
- **Правый клик по иконке Arch** — Запуск приложений fuzzel
- **XF86Mail** — Плавающее окно Proton Mail
- **SUPER+SHIFT+S** — Скриншот области (основной)
- **SUPER+SHIFT+W** — Скриншот окна
- **SUPER+SHIFT+F** — Скриншот полного экрана
- **Автоповтор клавиш включен** (40 скорость, 600 задержка для отзывчивого набора)
- **Все мультимедийные клавиши** — Громкость, яркость, управление воспроизведением

### Справка по горячим клавишам (SUPER+SHIFT+H)

- Локальная и динамичная: окно справки генерируется из вашей конфигурации Hyprland (встроенные комментарии на строках привязки).
- Источники:
    - ~/.config/hypr/keybindings.conf (предпочтительно)
    - ~/.config/hypr/hyprland.conf (запасной)
- Как добавить описания: добавьте комментарий после строки привязки, например bind = $mod, D, exec, fuzzel # App launcher
- Запуск: SUPER+SHIFT+H открывает компактное окно веб-приложения (Brave app mode), показывающее текущие привязки.
- Совет: Отредактируйте ваши привязки и снова нажмите SUPER+SHIFT+H для перегенерации.

### Стили рабочих пространств (Waybar)

Вы можете переключить стиль модуля рабочих пространств в вашей конфигурации Waybar. Отредактируйте ~/.config/waybar/config и измените имя модуля с:

- hyprland/workspaces#rw (по умолчанию: стиль «Rewrite» с иконками для каждого окна)

на любой из этих:

- hyprland/workspaces Circles (● ◉ ○)
- hyprland/workspaces#numbers Plain 1..4
- hyprland/workspaces#roman I II III IV
- hyprland/workspaces#kanji 一 二 三 四
- hyprland/workspaces#pacman Pac‑Man themed
- hyprland/workspaces#cam Uno/Due/Tre/Quattro
- hyprland/workspaces#4 Numbers + per‑workspace icons

Примечания:

- Постоянные рабочие пространства по умолчанию 1–4; 5–6 появляются автоматически при занятости. Чтобы изменить постоянство, настройте persistent-workspaces в config/waybar/ModulesWorkspaces.
- Безопасно перезагрузите панель после изменений:
  archriot --waybar-reload

### 🎨 **Работа с документами и медиа**

- **Gnome Text Editor** — По умолчанию для текстовых/markdown файлов (чистое, современное редактирование текста с темой CypherRiot)
- **Papers** — Средство просмотра PDF по умолчанию (современный просмотрщик документов GNOME)
- **MPV** — Воспроизведение видео с оптимальной производительностью
- **Улучшенная сеть waybar** — nmtui вместо impala для надежного управления Wi-Fi
- **Инструменты скриншотов** — интеграция grim/slurp/hyprshot для всех задач захвата
- **Запись экрана** — Kooha для простой записи экрана на основе GUI

### 🚫 **Удаленный мусор и корпоративные приложения**

- **Удалены инструменты 37signals/Basecamp** — Hey, веб-приложения Basecamp
- **Удалены корпоративные социальные сети** — Discord, проприетарные мессенджеры
- **Удалены тяжелые инструменты продуктивности** — Obsidian, LibreOffice, OBS Studio, KDEnlive, Pinta
- **Удалены проприетарные службы** — 1Password, Typora, Dropbox, Spotify, Zoom
- **Удалены развлекательные приложения** — веб-приложение YouTube, веб-приложение WhatsApp

<a id="system-management"></a>

## 🔄 Управление системой

### Обновления

**Обновления системы:**

```bash
sudo pacman -Syu                     # Стандартное обновление системы Arch Linux
yay -Syu                             # Обновление пакетов AUR
```

**Обновления ArchRiot:**

```bash
curl -fsSL https://ArchRiot.org/setup.sh | bash    # Обновить ArchRiot (то же самое, что и установка)
```

**Автоматические уведомления об обновлениях:** ArchRiot автоматически проверяет наличие обновлений каждые 4 часа и показывает диалог уведомления, когда доступны новые версии. Вы можете установить обновления, игнорировать уведомления или просто закрыть диалог.

Опции диалога обновления:

- Install from cURL — использует maintainer’s install/packages.yaml (может повторно ввести пакеты, которые вы ранее удалили).
- Install from Local — использует ваш локальный ~/.local/share/archriot/install/packages.yaml и уважает пакеты, которые вы удалили.

Когда выбирать что:

- Local: вы удалили пакеты, которые не хотите возвращать, вы настроили модули локально, или хотите самый безопасный апгрейд, сохраняющий ваш текущий набор.
- cURL: вы хотите выровняться с последним набором пакетов сопровождающего, вы чините сломанное локальное состояние, или вы предпочитаете канонические настройки по умолчанию после крупных изменений.

Дополнительно:

- View Diff — показывает унифицированный diff между вашим локальным packages.yaml и packages.yaml сопровождающего, чтобы вы могли предварительно просмотреть изменения перед обновлением.
- Локальная проверка безопасности — тест дымом запускается перед обновлениями «Local» и заблокирует, если обнаружит пакеты, которые будут reintroduced. Вы можете необязательно добавить в allowlist конкретные пакеты через ~/.config/archriot/upgrade-allowlist.txt.
- Bluesky (опционально) — PWA Bluesky не устанавливается по умолчанию. Чтобы включить его:

    ```bash
    mkdir -p ~/.local/share/applications
    cp ~/.local/share/archriot/config/applications/xtras/Bluesky.desktop ~/.local/share/applications/
    update-desktop-database ~/.local/share/applications
    ```

    Затем он появится в Fuzzel как "Bluesky."

Обертка Brave и флаги на пользователя

- Лаунчеры: только две видимые записи: Brave и Brave (Private). Внутренний archriot-brave.desktop скрыт и используется для сопоставления браузера по умолчанию; он не появится в лаунчерах.
- Обертка: лаунчер archriot-brave принудительно включает Wayland, когда доступен, и включает GPU rasterization по умолчанию для стабильности и производительности.
- Переопределения на пользователя: создайте ~/.config/archriot/brave-flags.conf (один флаг на строку), чтобы добавить или переопределить флаги. Примеры:
  --disable-gpu
  --ozone-platform=x11
- Приоритет: аргументы командной строки > brave-flags.conf > встроенные значения по умолчанию. Дублирующиеся флаги разрешаются по ключу (высший приоритет побеждает).
- PATH: обертка также доступна по адресу ~/.local/bin/archriot-brave для постоянного доступа из оболочки.
- Значения по умолчанию применяются автоматически (при работе на Wayland):
  --ozone-platform=wayland
  --enable-features=UseOzonePlatform
  --enable-gpu-rasterization

<div align="center">
<img src="config/images/upgrade.png" alt="ArchRiot Update Dialog" width="600">
<br><em>Уведомления об обновлении Waybar: 󰚰 (новое), 󱧘 (просмотренное), - (актуальное) с диалогом обновления в один клик</em>
</div>

Средство обновления ArchRiot загружает последнюю конфигурацию YAML и предварительно собранный бинарный файл, затем интеллектуально применяет только необходимые изменения. Система на основе YAML обеспечивает **атомарные обновления** с правильным разрешением зависимостей — никаких частичных сбоев или сломанных состояний, как у традиционных скриптовых средств обновления оболочки.

### Устранение неполадок порталов и скринкаста

- Подтвердите активные порталы:
    - systemctl --user status xdg-desktop-portal xdg-desktop-portal-hyprland
    - ps aux | grep xdg-desktop-portal
- Проверьте выбор предпочитаемого портала:
    - Проверьте ~/.config/xdg-desktop-portal/portals.conf (или /etc/xdg-desktop-portal/portals.conf)
    - Убедитесь, что default=hyprland;gtk; и hyprland выбран для ScreenCast/Screenshot/Clipboard
- Сбросьте порталы (исправьте устаревшие процессы):
    - systemctl --user restart xdg-desktop-portal xdg-desktop-portal-hyprland
    - Если необходимо: pkill xdg-desktop-portal; pkill xdg-desktop-portal-hyprland; затем перезапустите службы
- Быстрое тестирование скринкаста:
    - Kooha: kooha (запишите короткий клип). Вы должны увидеть запрос скринкаста Wayland и успешный захват на чистых установках.
    - OBS Studio: В Sources нажмите + и убедитесь, что "Screen Capture (PipeWire)" доступен. Выберите его и проверьте, работает ли захват монитора/окна. Если отсутствует, перезапустите порталы (см. выше) или перезапустите ваш сеанс Hyprland.
- Распространенные проблемы и исправления:
    - Убедитесь, что xdg-desktop-portal-hyprland установлен и обновлен
    - Избегайте конфликтующих порталов; убедитесь, что hyprland стоит первым в списке default
    - Если запросы не появляются, перезапустите пользовательский сеанс или перезапустите Hyprland

### Устранение неполадок переносимости Waybar

- Перезагрузка без дубликатов:
    - Отправьте команду перезагрузки конфигурации: archriot --waybar-reload (отправляет SIGUSR2; автоматически перезапускает Waybar при сбое)
    - Если панель не отвечает, перезапустите чисто: killall waybar; archriot --waybar-launch
- Проверьте состояние одного экземпляра и логи:
    - Статус: archriot --waybar-status
    - Логи: tail -n 200 ~/.cache/archriot/runtime.log
- Idle/Блокировка экрана не срабатывает? См. "Hypridle: Блокировка не срабатывает (синтаксис exec)" ниже
- Советы по динамическому обнаружению:
    - Сеть: модули/скрипты должны автоматически обнаруживать активные интерфейсы; убедитесь, что ваш интерфейс включен (ip link), и NetworkManager работает, если вы на него полагаетесь
    - Температура: скрипты сначала пробуют coretemp/k10temp/zenpower, затем thermal zones; установите lm_sensors и запустите sensors-detect, если температуры отсутствуют
    - Батарея: только для ноутбуков; проверьте, что /sys/class/power_supply/BAT\* существует
- Общие исправления:
    - После изменений темы/конфигурации предпочитайте перезагрузки SIGUSR2 (pkill -SIGUSR2 waybar) полному перезапуску для стабильности
    - Если модули выглядят устаревшими, перезапустите порталы (см. Устранение неполадок Порталов), а затем Waybar
    - Проверьте, что ваши конфиги существуют в ~/.config/waybar и что пользовательские скрипты являются исполняемыми

### Wi-Fi отключается при блокировке/простое

Если ваш Wi-Fi отключается, когда экран блокируется или во время простоя:

1. Быстрая диагностика (только чтение)

- Выполните:

```/dev/null/commands.sh#L1-3
~/.local/share/archriot/install/archriot --wifi-powersave-check
```

- Это выводит:
    - статус drop-in NetworkManager: ожидает wifi.powersave=2 в /etc/NetworkManager/conf.d/40-wifi-powersave.conf
    - Runtime power save на вашем беспроводном интерфейсе (например, wlan0): ожидает power_save off

2. Исправление drop-in NetworkManager (постоянное)

- Если drop-in отсутствует или не установлено в 2:

```/dev/null/commands.sh#L1-2
echo -e "[connection]\nwifi.powersave=2" | sudo tee /etc/NetworkManager/conf.d/40-wifi-powersave.conf
sudo systemctl reload NetworkManager
```

3. Отключите Runtime power save Wi-Fi (временно)

- Если диагностика показывает «power_save on» для вашего интерфейса:

```/dev/null/commands.sh#L1-1
sudo iw dev <iface> set power_save off
```

- Замените <iface> на ваш беспроводной интерфейс (например, wlan0). Это помогает избежать энергосбережения Wi-Fi, пока сеанс заблокирован.

4. Перезапустите проверку

- Проверьте и drop-in, и состояние runtime:

```/dev/null/commands.sh#L1-1
~/.local/share/archriot/install/archriot --wifi-powersave-check
```

### Резервное копирование и восстановление

```bash
migrate
```

**🎯 Migrate** — это отдельный проект от Cypher Riot, который автоматически устанавливается во время настройки ArchRiot. Это TUI (текстовый пользовательский интерфейс) **без опций командной строки**. Просто выполните команду и используйте интерактивное меню для:

- Создания полных резервных копий системы
- Восстановления из предыдущих резервных копий
- Миграции конфигураций между установками
- Сохранения всех ваших настроек

**Детали интеграции:** ArchRiot автоматически загружает и устанавливает последнюю версию Migrate из [CypherRiot/Migrate](https://github.com/CyphrRiot/Migrate) во время установки, гарантируя, что у вас всегда есть самые современные возможности резервного копирования без какой-либо ручной настройки.

## 🎨 Система тем CypherRiot

Есть только одна тема: **CypherRiot**, красивая тема в стиле Neo Tokyo Dark. Если она вам не нравится, файлы темы находятся в `~/.local/share/archriot/config/` и могут быть отредактированы.

**Визуальный дизайн:**

- **Стиль:** Кастомная эстетика Neo Tokyo Dark с темной элегантностью
- **Цветовая палитра:** Глубокие фиолетовые, электрические синие и угольно-серые фоны
- **Интеграция:** Полная системная тема (waybar, hyprlock, fuzzel, терминалы, приложения)
- **Фоны:** 23 обоев в стиле riot для динамической циклической смены

**Системная интеграция:**

- **Оконный менеджер:** Цвета CypherRiot в декорациях и границах Hyprland
- **Строка состояния:** Кастомный waybar с фиолетовыми акцентами CypherRiot и единообразным стилем
- **Экран блокировки:** Красивый hyprlock с темой CypherRiot и статусом системы
- **Приложения:** Единообразная тема в GTK, терминале и десктопных приложениях

### Управление обоями

- **Мгновенное применение:** Изменения фона применяются немедленно
- **Постоянные настройки:** Предпочтения фона переживают перезагрузки и обновления

**Система фона:**

- **Коллекция CypherRiot:** 23 обоев в стиле riot включены
- **Легкая смена:** Используйте `SUPER + CTRL + SPACE` для циклической смены фонов
- **Динамическое переключение:** Смена фона на основе скрипта для разнообразия
- **Высокое качество:** Подобранные обои, оптимизированные для эстетики CypherRiot

**Расширенная настройка:**

- **Расположение фонов:** `~/.local/share/archriot/backgrounds/` (объединенный каталог)
- **Пользовательские обои:** Добавьте свои обои в каталог backgrounds
- **Интеграция скриптов:** Смена фона интегрирована с waybar и статусом системы
- **Согласованность приложений:** Тема CypherRiot обеспечивает единообразный стиль во всех приложениях

## Понимание конфигурации YAML

**Для разработчиков и опытных пользователей, которые хотят настроить систему перед установкой:**

```bash
git clone https://github.com/CyphrRiot/ArchRiot.git ~/.local/share/archriot
# Отредактируйте install/packages.yaml для настройки
~/.local/share/archriot/install/archriot
```

### Архитектура YAML

ArchRiot использует современную систему конфигурации на основе YAML, которая заменяет традиционные сценарии оболочки. Вся система определена в `install/packages.yaml`, который содержит:

**Структура:**

- **Категории** (core, desktop, development, system, media)
- **Модули** внутри каждой категории (base, hyprland, tools и т.д.)
- **Каждый модуль определяет:**
    - `packages:` — Список пакетов для установки
    - `configs:` — Файлы конфигурации для развертывания с шаблонами и целями
    - `commands:` — Команды после установки для выполнения
    - `depends:` — Зависимости от других модулей
    - `type:` — Тип установки (pacman, yay, flatpak и т.д.)

**Пример модуля:**

```yaml
desktop:
    hyprland:
        packages: [hyprland, waybar, wofi, hyprpaper]
        configs:
            - pattern: "config/hypr/*"
              target: "~/.config/hypr/"
              preserve_if_exists: [monitors.conf]
            - pattern: "config/waybar/*"
              target: "~/.config/waybar/"
        commands: ["systemctl --user enable hyprland"]
        depends: [core.base]
        type: pacman
```

Эта система YAML обеспечивает чистое разделение пакетов, конфигураций и команд при сохранении полного разрешения зависимостей и правильного порядка установки.

### Сохранение конфигурации

ArchRiot включает в себя интеллектуальную систему сохранения через `install/preserve.yaml`, которая поддерживает ваши персональные настройки во время обновлений и переустановок. Это гарантирует, что раскладка клавиатуры, предпочтения приложений и другие настройки переживут обновления системы.

**Сохраняемые настройки:**

- **Конфигурация клавиатуры** — Раскладка (us, fr, de), вариант и модель
- **Приложения по умолчанию** — Браузер, терминал, файловый менеджер и предпочтения док-станции
- **Пользовательские настройки** — Любые настройки, которые вы изменили в конфигах Hyprland

**Как это работает:**

1. **Обнаружение** — Сканирует существующие конфиги на наличие настраиваемых пользователем настроек
2. **Резервное копирование** — Создает резервные копии с временной меткой в `~/.cache/archriot/`
3. **Извлечение** — Извлекает ваши персональные настройки с использованием определенных шаблонов
4. **Восстановление** — Применяет сохраненные настройки к новым конфигам (опционально)

**Пример записи в preserve.yaml:**

```yaml
user_customizable_settings:
    - name: "kb_layout"
      description: "Keyboard layout (us, fr, de, etc.)"
      pattern: "kb_layout"

    - name: "browser"
      description: "Default browser application"
      pattern: "$browser"
```

Система запрашивает во время установки: **"Restore your hyprland modifications?"**, позволяя вам выбрать, применять ли сохраненные предпочтения к свежей конфигурации.

## ⚡ ArchRiot одним взглядом

_Витрина — все, что делает ArchRiot особенным, в одном месте_

**🪟 Совершенство Wayland:** Композитор Hyprland с плавной анимацией, интеллектуальным тайлингом и нулевыми компромиссами XWayland

**💻 Рай разработчика:** Оболочка Fish, редактор Zed, Neovim, современные инструменты CLI (lsd, ripgrep) и контейнеры, которые просто работают

**🛡️ Арсенал конфиденциальности:** Браузер Brave, Proton Mail, Mullvad VPN, мессенджер Signal и Feather Wallet — все с нативной поддержкой Wayland

**🎨 Визуальное совершенство:** Темные темы CypherRiot, необязательный фильтр синего света и красивые интерфейсы, которые не портят глаза в 3 часа ночи

**⚡ Настроенная производительность:** Интеллектуальное управление памятью, аппаратное ускорение и оптимизированный аудио-стек для вычислений без задержек

## 🔀 Отличия от Omarchy

ArchRiot был (однажды) **тяжело настроенным форком** с этими ключевыми отличиями:

### Основная философия

- **Подход «Конфиденциальность прежде всего»** — Proton Mail, браузер Brave, мессенджер Signal против корпоративных альтернатив
- **Ориентация на разработчиков** — Редактор Zed, современные инструменты CLI, оболочка Fish, комплексное окружение для разработки
- **Производительность вместо раздутости** — Легкие приложения, интеллектуальное управление памятью, нативный Wayland
- **Чистая эстетика** — Тема CypherRiot, согласованная темная тема, минимум отвлекающих факторов

### Основные технические отличия

- **Встроенная система резервного копирования** — Интегрированный инструмент Migrate для полного резервного копирования/восстановления системы
- **Улучшенное управление окнами** — Адаптивное масштабирование в процентах на всех разрешениях
- **Комплексная поддержка GPU** — Автоматическое обнаружение и оптимизация для NVIDIA, AMD, Intel
- **Расширенная интеграция Waybar** — Пользовательские модули, таймер Pomodoro, статус VPN, мониторинг системы
- **Современный стек приложений** — Терминал Ghostty, браузер Brave, нативные приложения Wayland
- **Интеллектуальная настройка системы** — Исправления управления памятью, фильтр синего света, масштабирование DPI

ArchRiot превращает Omarchy из общей настройки продуктивности в специализированное окружение для разработки и с упором на конфиденциальность.

## 🔍 Система верификации установки

ArchRiot включает в себя комплексную систему верификации, чтобы гарантировать правильную работу всего:

### Что на самом деле делает установщик

Установщик ArchRiot автоматически проверяет все во время работы. Вам не нужна отдельная проверка, потому что установщик — ЭТО система проверки:

**Проверка в реальном времени во время установки:**

- Целостность конфигурации YAML и зависимости модулей
- Основные пакеты (yay, git, base-devel)
- Окружение рабочего стола (Hyprland, Waybar, fuzzel, mako)
- Развертывание файлов конфигурации
- Приложения (терминал, файловый менеджер, браузер, текстовый редактор)
- Системные службы (аудио, сеть, bluetooth)
- Сетевое подключение и доступность репозиториев
- Память, дисковое пространство и системные требования

**Если что-то не удается:** Установщик немедленно останавливается с четкими сообщениями об ошибках и диагностической информацией. Исправьте проблему и перезапустите — это полностью безопасно и идемпотентно.

- **Подробный анализ сбоев** с конкретными рекомендациями
- **Предложения по исправлению** для сбойных компонентов

### Верификация

Логи установки находятся в `~/.cache/archriot/install.log` и могут быть просмотрены для получения подробностей об установке или любых ошибках в процессе установки.

### Ожидаемые значения по умолчанию

После чистой установки вы должны увидеть:

- **Тема по умолчанию:** CypherRiot (эстетика Neo Tokyo Dark)
- **Фон по умолчанию:** riot_zero.png (обои в стиле riot)
- **Файлы PDF:** Показывают правильные иконки документов (не эскизы)
- **Файлы изображений:** Показывают эскизы в Thunar
- **Waybar:** Запущен с таймером помидора, статистикой системы и прозрачной кнопкой микрофона

## Флаги CLI ArchRiot

См. Расширенное использование: Флаги CLI для полного списка и подробностей.

[Переход к Расширенному использованию: Флаги CLI](#advanced-usage-archriot-cli-flags)

## Привязки Hyprland и exec-once (примеры)

- Exec-once (Waybar):
    - exec-once = $HOME/.local/share/archriot/install/archriot --waybar-launch
- Перезагрузка (Waybar):
    - bind = $mod SHIFT, SPACE, exec, $HOME/.local/share/archriot/install/archriot --waybar-reload
- Кошелек:
    - bind = $mod, R, exec, $HOME/.local/share/archriot/install/archriot --wallet
- Pomodoro:
    - bind = $mod, comma, exec, $HOME/.local/share/archriot/install/archriot --pomodoro-click

## Bluesky (опционально; только opt-in)

- Bluesky не установлен по умолчанию и не появляется в Fuzzel при чистой установке.
- Если предыдущая установка оставила desktop entry, установщик теперь удаляет его из:
    - `~/.local/share/applications/`, `/usr/local/share/applications/`, `/usr/share/applications/` (как Bluesky.desktop/bluesky.desktop)
    - Затем обновляет базу данных рабочего стола.

Включить Bluesky (opt-in):

```bash
cp ~/.local/share/archriot/config/applications/xtras/Bluesky.desktop ~/.local/share/applications/
update-desktop-database ~/.local/share/applications
```

<a id="advanced-usage-archriot-cli-flags"></a>

## 🧰 Расширенное использование: Флаги CLI

Эти флаги CLI первого класса заменяют устаревшие вспомогательные скрипты и используются напрямую в привязках клавиш Hyprland. Все они выполняют встроенный бинарный файл по адресу:
$HOME/.local/share/archriot/install/archriot

- --waybar-launch
    - Назначение: Лаунчер одного экземпляра с неблокирующей блокировкой и надежным ведением журнала.
    - Поведение: Гарантирует ровно один экземпляр Waybar; пишет логи в ~/.cache/archriot/runtime.log.
    - Пример (Hyprland exec-once): exec-once = $HOME/.local/share/archriot/install/archriot --waybar-launch

- --waybar-reload
    - Назначение: Надежная перезагрузка Waybar. Использует SIGUSR2, когда возможно; запасной вариант до контролируемого перезапуска.
    - Поведение: Устраняет дубликаты PID и избегает дублирования Waybar после перезагрузок/возобновления.
    - Пример (привязка по умолчанию): SUPER+SHIFT+SPACE → archriot --waybar-reload

- --wallet
    - Назначение: Фокус или запуск настроенного кошелька; избегает дублирования экземпляров.
    - Пример (привязка по умолчанию): SUPER+R → archriot --wallet

- --pomodoro-click
    - Назначение: Имитация клика по таймеру помидора в Waybar (переключение/сброс через поведение модуля).
    - Пример (привязка по умолчанию): SUPER+, → archriot --pomodoro-click

- --upgrade-smoketest
    - Назначение: Локальный тест дымом обновления, используемый диалогом обновления.
    - Коды выхода: 0 OK; 2 возможные повторения; 3 недоступно. Учитывает allowlist в ~/.config/archriot/upgrade-allowlist.txt

- --stay-awake
    - Назначение: Предотвратить перевод системы в спящий режим, пока выполняется задача; всегда отсоединяется, чтобы ваше приложение не было привязано к лаунчеру.
    - Использование:
        - archriot --stay-awake curl -L -o file.iso https://example.com/file.iso
        - archriot --stay-awake
    - Примечания: Использует systemd-inhibit для блокировки сна; не влияет на блокировку экрана.

- --volume
    - Назначение: Единое аудио-управление для динамиков и микрофона через PipeWire/PulseAudio.
    - Приоритет бэкенда: wpctl (PipeWire с отмеченным sink/source) → pamixer → pactl
    - Использование:
        - archriot --volume toggle # Переключить отключение звука динамика
        - archriot --volume inc # Увеличить громкость на 5%
        - archriot --volume dec # Уменьшить громкость на 5%
        - archriot --volume get # Получить текущий процент громкости

- --suspend-if-undocked
    - Назначение: Переводить в спящий режим только без док-станции (не подключен внешний дисплей) и не на AC/USB-PD; предназначен для менеджеров простоя.
    - Поведение: Ничего не делает, когда обнаружен внешний дисплей или AC/USB-PD; иначе вызывает systemctl suspend.
    - Пример (слушатель Hypridle): on-timeout = $HOME/.local/share/archriot/install/archriot --suspend-if-undocked

### Hypridle: Блокировка не срабатывает (синтаксис exec)

Симптомы:

- Экран никогда не блокируется после тайм-аута простоя, но `hyprlock` работает при ручном запуске.
- Подробные логи Hypridle показывают попытки запустить `exec, /usr/bin/hyprlock` и ошибки вроде `exec,: command not found`.

Причина:

- Стиль конфигурации Hyprland (`exec, ...`) был ошибочно использован внутри hypridle.conf.
- Hypridle ожидает либо `lock` (использует `lock_cmd`), либо прямой путь к исполняемому файлу. Он не интерпретирует `exec,`.

Исправление:

1. В `~/.config/hypr/hypridle.conf`, замените:

```ini
on-timeout = exec, /usr/bin/hyprlock
```

на либо:

```ini
on-timeout = lock
```

или (явный путь):

```ini
on-timeout = /usr/bin/hyprlock
```

2. Используйте абсолютные пути в `general {}` и слушателях, чтобы избежать проблем с PATH/env под Hyprland:

```ini
general {
  lock_cmd = /usr/bin/hyprlock
  before_sleep_cmd = /usr/bin/loginctl lock-session
  after_sleep_cmd = /usr/bin/hyprctl dispatch dpms on
}
```

3. Перезапустите hypridle:

```bash
pkill hypridle
hyprctl dispatch exec hypridle
```

Примечания:

- Модуль `idle_inhibitor` Waybar может блокировать простой. Убедитесь, что он не активирован, если блокировки не срабатывают.
- Затемнение яркости через 5 минут не повлияет на внешние мониторы HDMI/DP; рассмотрите возможность добавления DPMS off/on при блокировке для видимой подсказки на внешних мониторах.

- Устранение неполадок:
    - Проверьте бэкенд: wpctl status | head -30
    - Тест вручную: archriot --volume get
    - Waybar использует это для всех элементов управления громкостью (прокрутка, клик, микрофон)

- --brightness
    - Назначение: Управление подсветкой для дисплеев ноутбуков.
    - Использование:
        - archriot --brightness up # Увеличить яркость
        - archriot --brightness down # Уменьшить яркость
    - Примечания: Использует brightnessctl; интегрируется с действиями прокрутки модуля подсветки Waybar

- --startup-background
    - Назначение: Запустить обои при входе из сохраненных предпочтений; избегает применения темы во время загрузки для предотвращения гонок.
    - Поведение: Читает ~/.config/archriot/background-prefs.json (ключ: current_background), возвращается к riot_01.jpg или первому доступному изображению в ~/.local/share/archriot/backgrounds, записывает файл состояния в ~/.config/archriot/.current-background и перезапускает swaybg отсоединенно.
    - Пример (Hyprland exec-once): exec-once = $HOME/.local/share/archriot/install/archriot --startup-background

- --swaybg-next
    - Назначение: Переключить на следующие обои и обновить тему, если включена динамическая тема.
    - Поведение: Перебирает изображения в ~/.local/share/archriot/backgrounds, обновляет ~/.config/archriot/.current-background, перезапускает swaybg отсоединенно и запускает обновление темы по возможности.
    - Пример (привязка по умолчанию): SUPER+CTRL+SPACE → $HOME/.local/share/archriot/install/archriot --swaybg-next

- --waybar-workspace-click (опционально; рекомендуется использовать нативный on-click "activate" для правильной маршрутизации на монитор)
    - Назначение: Безопасный обработчик клика по рабочему пространству Waybar (только числовой).
    - Использование: $HOME/.local/share/archriot/install/archriot --waybar-workspace-click {name}
    - Примечания: Проверяет числовое рабочее пространство и отправляет: hyprctl dispatch workspace {name}
    - Пример (пользовательские модули): "on-click": "$HOME/.local/share/archriot/install/archriot --waybar-workspace-click {name} "

- --waybar-cpu
    - Назначение: Измеритель сводного использования ЦП для Waybar.
    - Поведение: Читает дельты /proc/stat; отображает полоску, процент и класс через JSON.
    - Пример (Waybar): "exec": "$HOME/.local/share/archriot/install/archriot --waybar-cpu"

- --waybar-temp
    - Назначение: Измеритель температуры ЦП для Waybar.
    - Поведение: Автоопределение датчика (hwmon coretemp/k10temp/zenpower → temp1_input; x86_pkg_temp thermal zone; thermal_zone0 fallback), отображает полоску и класс через JSON.
    - Пример (Waybar): "exec": "$HOME/.local/share/archriot/install/archriot --waybar-temp"

- --waybar-volume
    - Назначение: Измеритель громкости динамика для Waybar.
    - Приоритет бэкенда: wpctl (PipeWire) → pamixer → pactl. Отображает полоску и иконку через JSON; благополучно показывает «audio not ready».
    - Пример (Waybar): "exec": "$HOME/.local/share/archriot/install/archriot --waybar-volume"

- --waybar-memory
    - Назначение: Измеритель использования памяти для Waybar.
    - Поведение: Вычисляет традиционный процент, показывает современный против традиционного во всплывающей подсказке; отображает полоску и класс через JSON.
    - Пример (Waybar): "exec": "$HOME/.local/share/archriot/install/archriot --waybar-memory"

- --stabilize-session
    - Назначение: Утилита восстановления сеанса. Устраняет дубликаты Waybar и перезапускает один управляемый экземпляр; перезапускает hypridle, чтобы гарантировать активность политики простоя/блокировки.
    - Использование:
        - archriot --stabilize-session
        - archriot --stabilize-session --inhibit # запускает отсоединенный ингибитор сна для длительной работы

- --zed
    - Назначение: Фокус или запуск Zed (нативный > Flatpak) с дружественной средой Wayland; фокусирует существующее окно, если оно присутствует.
    - Пример (привязка): SUPER+Z → $HOME/.local/share/archriot/install/archriot --zed
    - Пример (Desktop entry Exec): $HOME/.local/share/archriot/install/archriot --zed %U

- --welcome
    - Назначение: Запуск окна приветствия ArchRiot (Python GTK) отсоединенно.
    - Пример (Hyprland exec-once): sleep 2 && $HOME/.local/share/archriot/install/archriot --welcome

### Примеры привязок Hyprland (копипаст)

Они уже присутствуют по умолчанию; используйте, если вам нужно применить или протестировать в реальном времени.

```bash
# Безопасно перезагрузить Waybar
hyprctl keyword bind "$mod SHIFT, SPACE, exec, $HOME/.local/share/archriot/install/archriot --waybar-reload"

# Кошелек
hyprctl keyword bind "$mod, R, exec, $HOME/.local/share/archriot/install/archriot --wallet"

# Pomodoro
hyprctl keyword bind "$mod, comma, exec, $HOME/.local/share/archriot/install/archriot --pomodoro-click"

# Telegram (с разрешением PATH; показывает 2s уведомление 'Opening Telegram...')
hyprctl keyword bind "$mod, G, exec, sh -lc 'notify-send -t 2000 "Telegram" "Opening Telegram..." >/dev/null 2>&1; Telegram'"
```

## Обертка Brave и сопоставление обработчиков

ArchRiot направляет все запуски браузера через обертку с разрешением PATH: `archriot-brave`.

- Разрешение исполняемого файла:
    - Установщик гарантирует, что обертка находится в PATH.
    - Любой устаревший `/usr/local/bin/archriot-brave` проактивно удаляется при установке/обновлении, чтобы избежать затенения.

- Значения по умолчанию и переопределения:
    - Значения по умолчанию включают Wayland/Ozone и GPU rasterization.
    - Файл флагов пользователя: `~/.config/archriot/brave-flags.conf`
    - Приоритет флагов: флаги CLI > файл флагов пользователя > значения обертки по умолчанию
    - Пример (файл флагов пользователя; один флаг на строку):
      --ozone-platform-hint=wayland
      --enable-gpu-rasterization

- Проверьте аппаратное ускорение:
    - Откройте: `brave://gpu`
    - Ожидание: Большинство функций показывают «Hardware accelerated».
    - Если вы отлаживаете проблемы, вы можете временно запустить в безопасном режиме:
      archriot-brave --disable-gpu
    - Сбой при переключении рабочего пространства/монитора: протестируйте с безопасным режимом выше. Если стабильно, оставьте `--disable-gpu` временно и проверьте драйверы GPU (Mesa/NVIDIA) и флаги Wayland; сообщите о вашей комбинации GPU/драйвера.

- Политика и проверка обработчиков:
    - HTTP/HTTPS должны разрешаться к обертке через desktop entry Brave.
    - Проверьте текущие значения по умолчанию:
      xdg-settings get default-web-browser
      xdg-mime query default x-scheme-handler/http
      xdg-mime query default x-scheme-handler/https
    - Ожидание: Desktop entry обертки ArchRiot является значением по умолчанию (`archriot-brave.desktop`). Только «Brave» и «Brave (Private)» должны появляться как видимые обработчики в общих меню.

Примечания:

- Этот подход с оберткой избегает ловушек расширения `$HOME` и сохраняет согласованную конфигурацию Wayland по умолчанию.
- Используйте флаги CLI для разовых тестов; предпочитайте файл флагов пользователя для постоянных изменений.

### Мульти‑монитор (опциональные флаги)

Некоторым настройкам Wayland с несколькими мониторами полезны эти флаги. Добавьте их в ~/.config/archriot/brave-flags.conf (по одному на строку), затем перезапустите Brave:

```
--disable-features=WaylandWpColorManagerV1
--enable-features=VaapiVideoDecoder,VaapiIgnoreDriverChecks,Vulkan,DefaultANGLEVulkan,VulkanFromANGLE,UseOzonePlatform
--ozone-platform=wayland
--enable-gpu-rasterization
--enable-zero-copy
--ignore-gpu-blocklist
```

Совет:

- Тестируйте постепенно и оставляйте только то, что вам нужно.
- Приоритет флагов: аргументы командной строки > brave‑flags.conf > значения по умолчанию.

## Waybar: Логи, перезагрузки и отладка

См.:

- 🧰 Расширенное использование: Флаги CLI — команды waybar (запуск/перезагрузка/статус)
- 🔧 Устранение неполадок — путь к логам, перезагрузки SIGUSR2, советы по дедупликации и шаги восстановления

<a id="troubleshooting"></a>

## 🔧 Устранение неполадок

### Отказ от принудительного применения настроек дисплея

Если ваша конфигурация дисплея не должна применяться автоматически (например, чтобы предотвратить отключение панели вашего ноутбука), вы можете безопасно отказаться:

- Отключить принудительное применение (никакие изменения монитора не будут применены):
    - Создайте файл-маркер:
        - touch ~/.config/archriot/disable-display-enforcement

- Включить принудительное применение:
    - Удалите файл-маркер:
        - rm ~/.config/archriot/disable-display-enforcement

Примечания:

- Служба времени входа не будет ничего делать, когда этот маркер существует.
- Вы все еще можете запускать ручные команды, когда необходимо:
    - ~/.local/share/archriot/install/archriot --displays-enforce
    - ~/.local/share/archriot/install/archriot --waybar-reload

### Экран блокировки: Погода и Криптовалюта

- Погода (включить/отключить)
    - Отключить: touch ~/.config/archriot/disable-weather
    - Включить: rm ~/.config/archriot/disable-weather
    - Необязательная конфигурация местоположения (используйте любой файл):
        - ~/.config/waybar/weather.conf
        - ~/.config/archriot/weather.conf
        - Поместите строку: LOCATION="Город, CC"

- Криптовалюта (включить/отключить)
    - Отключить: touch ~/.config/archriot/disable-crypto
    - Включить: rm ~/.config/archriot/disable-crypto
    - Настроить монеты: ~/.config/archriot/crypto.cfg
        - CoinGecko id через пробел или запятую (до 5), например: bitcoin ethereum solana

- Примечания
    - Погода использует stormy; если отсутствует, просто ничего не показывает (без ошибок).
    - Криптовалюта использует curl + jq + CoinGecko; если зависимости отсутствуют, просто ничего не показывает.

### Аномалии с несколькими мониторами после установки (несовпадение ABI)

Если вы видите артефакты дисплея или сбои на настройках с несколькими мониторами сразу после установки или обновления, это скорее всего несоответствие ABI частичного обновления (например, Hyprland/wlroots/portals не синхронизированы). Исправьте это, выполнив полное обновление, затем снова запустите установщик:

```bash
sudo pacman -Sy archlinux-keyring && yay -Syu && yay -Yc && sudo paccache -r
```

Совет профи: запустите установщик с `--strict-abi`, чтобы заблокировать установку, пока не будут применены обновления композитора/Wayland.

### Восстановление синхронизации установщика (блокировка db pacman / зеркала)

Если вы видите ошибки pacman, такие как «could not lock database» или «failed to synchronize», попробуйте эти шаги:

Симптомы:

- could not lock database (db.lck present)
- failed to synchronize packages / failed retrieving file
- temporary failure in name resolution

Шаги:

1. Убедитесь, что никакой другой менеджер пакетов не запущен (pacman/yay/paru).
2. Очистите устаревшую блокировку (безопасно, если pacman не запущен):
   sudo rm -f /var/lib/pacman/db.lck
3. Обновите базы данных:
   sudo pacman -Sy
    # Если проблемы сохраняются, принудительно выполните полное обновление:
    sudo pacman -Syy
4. Повторите попытку установки/обновления и следите за кратковременными проблемами зеркала/сети.

### Предварительный аудит (только чтение)

- Выполните: `~/.local/share/archriot/install/archriot --preflight`
- Что он проверяет (никаких изменений не вносится; безопасно запускать в любое время):
    - Config: проверяет `packages.yaml`
    - Binary path: подтверждает, что вы используете `$HOME/.local/share/archriot/install/archriot`
    - Hyprland binds: проверяет `SUPER+G` (Telegram) и `SUPER+S` (Signal)
    - Exec-once: гарантирует, что Waybar использует `archriot --waybar-launch`
    - Memory tuning: показывает статус opt-in (не изменяет настройки ядра)
    - Waybar: показывает количество экземпляров (не убивает процесс); логи в ~/.cache/archriot/runtime.log

### Настройка памяти (Базовые настройки по умолчанию; Расширенные по желанию)

По умолчанию ArchRiot отключает ZRAM, чтобы предотвратить треш-сжатой ОЗУ и зависания под нагрузкой, и применяет минимальные, безопасные настройки VM system‑wide (vm.zone_reclaim_mode=0 и vm.overcommit_memory=0). Чтобы повторно включить ZRAM, удалите или отредактируйте /etc/systemd/zram-generator.conf (установите zram-size в ненулевое значение), и если вы используете systemd-swap, включите его с помощью: sudo systemctl enable --now systemd-swap.service; затем перезагрузитесь. Для расширенной динамической настройки (включая vm.min_free_kbytes ≈ 1% of RAM capped at 256MB, swappiness/dirty ratios), выберите (opt-in), создав файл-флаг и перезапустив установку/обновление. Опционально (opt-in): touch ~/.config/archriot/enable-memory-optimizations и перезапустите установщик/обновление; проверьте, что /etc/sysctl.d/99-memory-optimization.conf содержит вычисленное vm.min_free_kbytes.

```bash
touch ~/.config/archriot/enable-memory-optimizations
```

Применить сейчас (opt-in):

```bash
sudo cp ~/.local/share/archriot/config/system/99-memory-optimization.conf /etc/sysctl.d/99-memory-optimization.conf
total_kb=$(awk '/MemTotal/ {print $2}' /proc/meminfo); calc=$(awk -v t="$total_kb" 'BEGIN {m=int(t*0.01); if (m > 262144) m=262144; print m}'); sudo sed -i "s/^vm.min_free_kbytes=.*/vm.min_free_kbytes=$calc/" /etc/sysctl.d/99-memory-optimization.conf
sudo sysctl -p /etc/sysctl.d/99-memory-optimization.conf
```

Быстро откатить (если что-то ощущается неправильно):

```bash
sudo sed -i 's/^vm.overcommit*memory=.*/vm.overcommit*memory=0/; s/^vm.overcommit_ratio=.*/vm.overcommit_ratio=50/; s/^vm.min_free_kbytes=.*/vm.min_free_kbytes=262144/' /etc/sysctl.d/99-memory-optimization.conf
sudo sysctl -p /etc/sysctl.d/99-memory-optimization.conf
```

### Мигающий курсор вместо Hyprland

Если ваша система загружается до **мигающего курсора** вместо запуска Hyprland:

1. **Попадите в терминал:** Нажмите `CTRL+ALT+F3`
2. **Войдите** с вашим именем пользователя и паролем
3. **Перезапустите установщик** для исправления проблем с GPU/графикой:
    ```bash
    curl -fsSL https://ArchRiot.org/setup.sh | bash
    ```
4. **Перезагрузитесь** после завершения скрипта

Эта проблема почти всегда связана с GPU, и установщик автоматически обнаружит и исправит проблемы с графическими драйверами.

### Устранение неполадок Wi-Fi ISO (Mediatek MT7921K)

Живое исправление без пересборки ISO. Применяйте в таком порядке:

- Включите iwd и избегайте конфликтов:
  sudo systemctl enable --now iwd
  sudo systemctl mask wpa_supplicant

- Разблокируйте радио:
  rfkill list
  sudo rfkill unblock all

- Перезагрузите драйвер Mediatek с отключенным ASPM (исправление стабильности):
  sudo modprobe -r mt7921e
  sudo modprobe mt7921e disable_aspm=1
  Если модуль отказывается выгружаться, сначала отключите интерфейс (через iwctl), затем повторите попытку.

- Проверьте отсутствие прошивки:
  dmesg | grep -i firmware
  Если вы видите сообщения об отсутствии прошивки Mediatek:
    - На установленной системе с сетью:
      sudo pacman -Syu linux-firmware
      sudo modprobe -r mt7921e && sudo modprobe mt7921e disable_aspm=1
    - На live ISO без сети:
      Скопируйте необходимые файлы прошивки Mediatek на /lib/firmware/mediatek с другой машины/USB, затем перезагрузите модуль.

- Подключитесь, используя iwd (iwctl):
  iwctl
  device list
  station {device} scan
  station {device} get-networks
  station {device} connect {network}
  station {device} show
  exit

- Сохраните после установки (рекомендуется):
  Создайте /etc/modprobe.d/mt7921e.conf с:
  options mt7921e disable_aspm=1

- Проверьте:
    - Устройство PCIe привязано к mt7921e:
      lspci -k | grep -A3 -i 7921
    - Радио разблокировано:
      rfkill list
    - Ссылка видна/подключена:
      iw dev

### Предотвратить сон простоя и сохранить загрузки активными

Если ваша система все еще засыпает после ~10 минут и прерывает загрузки, убедитесь, что сон простоя отключен в logind и что Hypridle не переводит в спящий режим раньше ожидаемого.

1. Проверьте политику простоя logind (без сна простоя)

- Подтвердите, что drop-in ArchRiot существует и отключает действие простоя:

```bash
cat /etc/systemd/logind.conf.d/20-idle-ignore.conf
# Expect:
# [Login]
# IdleAction=ignore
```

- Проверьте эффективные настройки:

```bash
loginctl show-logind -p IdleAction -p IdleActionUSec
# Expect: IdleAction=ignore
```

- Если отсутствует, создайте drop-in (требуется перезагрузка для вступления в силу):

```bash
sudo mkdir -p /etc/systemd/logind.conf.d
printf "[Login]\nIdleAction=ignore\n" | sudo tee /etc/systemd/logind.conf.d/20-idle-ignore.conf
# Reboot to apply, or be aware that restarting logind can disrupt your session:
# sudo systemctl restart systemd-logind
```

2. Поведение Hypridle (блокировка vs спящий режим)

- Предполагаемые значения по умолчанию ArchRiot:
    - Блокировка через 10 минут
    - Спящий режим через 30 минут, только без док-станции (нативный: archriot --suspend-if-undocked; запасной: suspend-if-undocked.sh)
- Проверьте, запущен ли Hypridle:

```bash
pgrep -a hypridle || echo "hypridle not running"
```

- Временно остановите Hypridle (для тестирования):

```bash
pkill hypridle
```

- Если вы хотите, чтобы загрузки продолжались (без авто-сна):
    - Отредактируйте вашу конфигурацию Hypridle (обычно в ~/.config/hypr/hypridle.conf), чтобы удалить или закомментировать действие on-timeout suspend, которое вызывает suspend-if-undocked.sh.
    - Оставьте действие блокировки на 10 минут, если хотите; удалите только действие сна.

3. Сделайте сон опциональным во время длительных загрузок (ингибитор)

- Используйте одноразовый ингибитор сна при запуске загрузки:

```bash
systemd-inhibit --what=sleep --why="Active download" your-download-command-here
```

- Или держите сеанс бодрствующим, пока вы не отмените:

```bash
systemd-inhibit --what=sleep --why="Keep awake for downloads" bash -c 'while :; do sleep 300; done'
```

4. Необязательное жесткое отключение целей системного сна

- Если вы хотите полностью предотвратить сон system-wide:

```bash
sudo systemctl mask sleep.target suspend.target hibernate.target hybrid-sleep.target
```

- Включить позже:

```bash
sudo systemctl unmask sleep.target suspend.target hibernate.target hybrid-sleep.target
```

5. Контрольный список проверки

- Logind показывает игнорирование простоя:

```bash
loginctl show-logind -p IdleAction -p IdleActionUSec
```

- Отсутствует активное действие suspend Hypridle; настроена только блокировка (или Hypridle не работает во время длительных загрузок).
- Ингибиторы показывают ожидаемые блокировщики во время загрузок:

```bash
systemd-inhibit --list
```

- Система остается бодрствующей сверх вашего предыдущего тайм-аута (например, запустите 20-минутный тест no-op):

```bash
timeout 1200 bash -c 'date; sleep 1200; date'
```

Примечание:

- ArchRiot не перезапускает systemd-logind во время установки/обновления. После создания или изменения drop-ins в /etc/systemd/logind.conf.d, перезагрузитесь для чистого применения.
- Сохранение блокировки на 10 минут при отключенном сне сохраняет безопасность без прерывания передач.

## 🛠️ Инструменты разработки

_Для участников и опытных пользователей, которые хотят собирать из исходного кода_

```bash
# Собрать ArchRiot из исходного кода (в каталоге репозитория)
make                                 # Собрать бинарный файл Go
make test                            # Запустить набор тестов
make dev                             # Сборка для разработки

# Проверка версии
cat ~/.local/share/archriot/VERSION  # Показать установленную версию ArchRiot
```

## 📂 Информация о репозитории

- **Основной репозиторий:** [https://github.com/CyphrRiot/ArchRiot](https://github.com/CyphrRiot/ArchRiot)
- **Сопровождение:** Активное, с регулярными обновлениями и улучшениями
- **Сообщество:** Открыто для вопросов, предложений и вкладов

## 🌐 Связь с CyphrRiot

<div align="center">

**Оставайтесь на связи для обновлений, советов и совершенства Linux**

[![Follow CyphrRiot on X](https://img.shields.io/badge/Follow-@CyphrRiot-1DA1F2?style=for-the-badge&logo=x&logoColor=white)](https://x.com/CyphrRiot)
[![GitHub Profile](https://img.shields.io/badge/GitHub-CyphrRiot-181717?style=for-the-badge&logo=github&logoColor=white)](https://github.com/CyphrRiot)

_Создание системы Linux, которую вы действительно хотите использовать_

</div>

## 📋 Системные требования

- **Свежая установка Arch Linux** (рекомендуется)
- **Интернет-соединение** для загрузки пакетов
- **4ГБ+ ОЗУ** (8ГБ+ рекомендуется для разработки)
- **8ГБ+ места на диске** (10ГБ+ для полной среды разработки)
- **ЦП:** Любой современный процессор (оптимизирован как для Intel/AMD)
- **GPU:** Любая современная видеокарта (см. раздел Поддержка GPU для полных деталей совместимости)

_Примечание: ArchRiot начался как уникальный rice[^1] и эволюционировал от установщика Omarchy от DHH [DHH's Omarchy](https://omarchy.org/), но с тех пор стал совершенно отдельным дистрибутивом Linux на базе Arch. Он включает в себя пользовательский установщик, расширенную систему управления пакетами на основе Go и множество пользовательских приложений и инструментов, ориентированных на конфиденциальность, продуктивность разработчика и чистую эстетику._

[^1]: В контексте Linux, "rice" — это сленг для настройки или изменения рабочего стола или пользовательского интерфейса, чтобы он выглядел эстетически приятно или высоко персонализировано, часто с упором на минимализм, уникальные темы или облегченные настройки. Он происходит от термина "ricer", первоначально использовавшегося в автомобильной культуре для описания сильно модифицированных автомобилей (вдохновлено "rice burner" для японских автомобилей).

## 🖥️ ВМ и аппаратные заметки

**ArchRiot разработан для «голого железа».** Хотя он отлично работает в ВМ, вы упускаете полный опыт. Эта система создана для замены любого разочаровывающего дистрибутива Linux, в котором вы застряли. Установите его на реальное железо, где ему и место.

**Для тестирования в ВМ:**

- **VirtualBox/VMware:** Работает из коробки с ArchRiot ISO
- **QEMU/KVM:** Полная поддержка ускорения с драйверами virtio
- **Рекомендуемые характеристики ВМ:** 4ГБ+ ОЗУ, 20ГБ+ места, загрузка EFI включена

**Совместимость оборудования:**

- **Системы с несколькими GPU:** Автоматическое обнаружение и установка драйверов
- **Дисплеи высокого DPI:** Правильное масштабирование во всех приложениях
- **Игровое железо:** Полная поддержка графики NVIDIA/AMD/Intel
- **Современные ноутбуки:** Управление батареей, подсветкой и профили питания

### Дробное масштабирование (Wayland) — Поведение и советы

- Масштабирование на монитор поддерживается и настраивается через мониторы Hyprland (редактируется в `~/.config/hypr/monitors.conf`) или Настройки дисплея Панели управления.
- Нативные приложения Wayland отображаются четко при дробных масштабах (например, 0.9, 1.25). Приложения XWayland могут выглядеть немного мягко при нецелых масштабах.
- Brave/PWAs: Обертка ArchRiot Brave устанавливает `--force-device-scale-factor=1` и `--high-dpi-support=1`, чтобы предотвратить уменьшение контента при масштабах ниже 1.0. Используйте зум на сайт/страницу в Brave для точной настройки при масштабах >1.0.
- Смешанный DPI (мульти-монитор): Предпочитаете масштабирование Wayland на монитор; избегайте глобальных хаков Xft.dpi. Используйте профили kanshi, если вы часто переключаете мониторы.
- Проверка:
    - `hyprctl monitors` показывает текущий масштаб на дисплей
    - Waybar и приложения GTK должны масштабироваться чисто при регулировке масштаба монитора
    - Скриншоты отражают масштабированную геометрию, как ожидается
- Известные особенности:
    - Некоторые приложения Electron под XWayland могут выглядеть мягко при дробных масштабах — устанавливайте сборки Wayland, когда они доступны
    - Устаревшие тулкиты могут игнорировать дробное масштабирование; рассмотрите альтернативы или запускайте с конкретными флагами тулкита, если необходимо

## Матрица QA — Валидация релиза 3.5

Оборудование

- Intel iGPU (современный/Arc), AMD Radeon, NVIDIA proprietary

Дисплеи

- Один монитор
- Мульти‑монитор (док/без док-станции)
- Дробное масштабирование (0.9, 1.25)

Локаль

- en‑US
- Одна не-латинская (например, ja‑JP или ar)

Контрольный список валидации

- [ ] Waybar один экземпляр работает; перезагрузка сначала SIGUSR2; ~/.cache/archriot/runtime.log присутствует
- [ ] Порталы OK: запрос скринкаста Kooha работает; OBS показывает «Screen Capture (PipeWire)»
- [ ] Привязки справки: SUPER+H открывает сайт; SUPER+SHIFT+H показывает локальную справку с текущими привязками
- [ ] Thunar «Открыть терминал здесь» запускает Ghostty
- [ ] Zed фокус или запуск (нативный > Flatpak) через SUPER+Z
- [ ] Диалог обновления: неблокирующий; копия включает путь к логам
- [ ] Обертка Brave: brave://gpu показывает ускорение; безопасный режим исправляет любой сбой при переключении рабочего пространства/монитора
- [ ] Дробное масштабирование ведет себя как задокументировано; контент Brave не уменьшен при <1.0
- [ ] Окно Панели управления масштабируется (60% x 85%) на мониторах/масштабах
- [ ] Скриншоты (опционально): подтверждения захвата для обновления README

**Вы закончили!** Если установщик завершился, ваша система ArchRiot готова к работе. Перезагрузитесь и наслаждайтесь вашим идеально настроенным рабочим столом.

**Что-то не работает?** Перезапустите установщик — он разработан для устранения проблем и обслуживания вашей системы.

🎉 Спасибо [Vaxryy](https://x.com/vaxryy) за создание Hyprland — композитора, который не отстой.

И спасибо JaKoolIt за [ваши удивительные скрипты](https://github.com/JaKooLit/Arch-Hyprland)!

## 👥 Участники

- Tarso Galvão (surtarso)
    - GitHub: https://github.com/surtarso
    - Примечные вклады: Расширенные стили рабочих пространств Waybar (ModulesWorkspaces), сопоставление иконок для каждого окна, соответствующая полировка конфигурации Waybar.

## 🧱 Архитектура проекта

ArchRiot структурирован так, что точка входа (`source/main.go`) только анализирует флаги CLI и делегирует модульным пакетам. Это сохраняет ядро небольшим, тестируемым и безопасным для эволюции.

- Точка входа (только делегирование)
    - `source/main.go`
        - Анализирует флаги
        - Делегирует функциям пакета
        - Не содержит логики функций; только минимальный клей

- Основные помощники сеанса
    - `source/session/`
        - Жизненный цикл Waybar: `LaunchWaybar`, `ReloadWaybar`, `SweepWaybar`
        - Коалесцент перезагрузки для Hyprland: `ReloadHyprland`, `ReloadHyprlandImmediate`
        - Питание и стабильность: `PowerMenu`, `StabilizeSession`, `RebootNow`
        - Охрана окружения и потоки: `SuspendGuard`, `MullvadStartup`
        - Помощники UX: `PomodoroClick`, `PomodoroDelayToggle`, `WorkspaceClick`, `WelcomeLaunch`, `StartupBackground`

- Темизация
    - `source/theming/` + `source/theming/applications/`
        - Реестр тем и приложение (например, Waybar, Ghostty, Hyprland, Text Editor)
        - Интегрировано с коалесцентом перезагрузки, чтобы избежать двойной перезагрузки

- Инструменты дисплея и рабочего пространства
    - `source/displays/` (например, `Autogen` для kanshi)
    - `source/waybartools/` (например, `SetupTemperature`)
    - `source/windows/` (например, `Switcher`, `FixOffscreen`)

- Обновление, ограждения и безопасная загрузка
    - `source/upgradeguard/` (например, `PreInstall` защита ABI для композитора/Wayland)
    - `source/upgrunner/` (например, `Bootstrap` оркестрация потока обновления)
    - `source/secboot/` (например, `RunContinuation`, `RestoreHyprlandConfig`)

- Утилиты CLI и помощники установщика
    - `source/cli/` (например, `ShowHelp`, `ValidateConfig`)
    - `source/installer/` (например, `EnsureYay` начальная загрузка)
    - `source/executor/`, `source/orchestrator/` (оркестрация установки)

Ключевые правила (принудительно в коде и процессе)

- `main.go` — это только точка входа: анализ флагов → делегирование пакетам.
- Каждый флаг CLI отображается на выделенную функцию пакета (нет логики функций в `main.go`).
- Одно изменение за раз; всегда запускайте `make` после каждого изменения и фиксируйте точные файлы.
- Немедленно удаляйте недостижимый или дублирующийся код, чтобы предотвратить дрейф.

## ✨ Что нового в v3.9.2

- Новая чистка памяти первого класса:
    - `~/.local/share/archriot/install/archriot --memory-clean`
    - Опции:
        - `--if-low` (чистить только когда MemAvailable < 1024 MB по умолчанию)
        - `--threshold-mb N` (настроить порог)
        - `--notify`
    - Повышение прав происходит автоматически: root → pkexec (GUI) → sudo fallback

- Необязательная стабилизация при низком уровне памяти (без перезагрузки композитора):
    - `~/.local/share/archriot/install/archriot --stabilize-session --memory-clean --notify`

- Необязательная привязка клавиши (отключена по умолчанию, если вы не включите ее):
    - `bind = $mod CTRL SHIFT, K, exec, $HOME/.local/share/archriot/install/archriot --stabilize-session --memory-clean --notify  # Stabilize (low-memory)`

## ✨ Что нового в v3.6

- Смягчение зависания установщика: тайм-ауты для каждой команды с захватом вывода для предотвращения задержек; некритические команды продолжают выполнение, критические команды быстро fail с четкими логами. Расширенные тайм-ауты для операций pacman/yay.
- Надежность Hypridle: автозапуск теперь использует абсолютный путь (/usr/bin/hypridle); ensureIdleLockSetup добавляет то же самое, чтобы избежать проблем с таймингом PATH/env после обновлений.
- Безопасность GTK Панели управления: повторное применение настроек запускается только при обнаружении сеанса GUI; принудительно включает GSK_RENDERER=gl; автоматический откат на software-renderer cairo, если GL не удается, чтобы избежать ошибок контекста OpenGL GTK.
- Флаги мульти-монитора Brave: документированы необязательные флаги для стабильности/производительности Wayland с несколькими мониторами в разделе Обертка Brave.
- Полировка документации: исправлено оглавление/глубокие ссылки; удалено дублирование (Настройка памяти); обновлен бейдж версии и Текущий релиз до v3.6.1.

## ✨ Что нового в v3.5 (Документация и UX)

- 🔑 Справка по горячим клавишам (SUPER+SHIFT+H): локальное, динамичное окно справки, сгенерированное из вашей конфигурации Hyprland; открывается как компактное окно «приложения» Brave. Доступен откат GTK.
- ✉️ Telegram (SUPER+G): поведение фокуса или запуска теперь отражает Fuzzel; включает устойчивую повторную попытку с задержкой при холодном запуске.
- 🛡️ Mullvad во время обновления: защита авто-переподключения, если вы начинаете обновление подключенным (без перезапуска служб или изменений NM).
- 🧩 Защита установщика (несоответствие ABI): предупреждает, если ожидаются обновления Hyprland/wlroots/portal/Wayland; используйте `--strict-abi`, чтобы заблокировать установку, пока система не будет обновлена.
- 🔋 Батарея «Полная»: показывает 100%, когда контроллер сообщает «fully-charged» для более понятного UX.
- 📚 Полировка README: эмодзи «Навигация по этому руководству», панель быстрой установки и глубокий технический контент консолидированы в Расширенное использование и Устранение неполадок.

<a id="license"></a>

## 📄 Лицензия

ArchRiot выпущен под [MIT License](https://opensource.org/licenses/MIT), что позволяет вклад сообщества и модификации.

# 🛡️⚔️🪐 Hack the Planet 🪐⚔️🛡️

# Примечания к выпуску ArchRiot 3.4

Сосредоточены на едином шрифте по умолчанию, полировке UI и дружественном к Wayland масштабировании.

## Основные моменты

- Paper Mono как системный моноширинный шрифт по умолчанию
    - Поставляется в config/fonts и устанавливается в ~/.local/share/fonts
    - Применен в GTK3/GTK4, Waybar, Fuzzel, Ghostty, Панель управления, Приветствие и Zed
    - Покрытие символов через ttf-nerd-fonts-symbols сохранено для иконок/глифов
- Веб-приложения и окна Brave правильно масштабируются при дробных масштабах монитора
    - Добавлено --force-device-scale-factor=1 --high-dpi-support=1 в Brave (Wayland)
    - Предотвращает уменьшение контента внутри PWA, когда масштаб монитора < 1.0 (например, 0.9)
- Thunar «Открыть терминал здесь» запускает Ghostty напрямую
    - Работает надежно, даже если не настроены предпочтительные приложения exo
- Секция обоев Панели управления больше не уходит за пределы экрана
    - Слайдер фона обернут в горизонтальный скроллер
    - Метка текущего обоев переносится/усекается для сохранения макета

### Заслуги и использование шрифта

Paper Mono от Paper Design используется как системный моноширинный шрифт по умолчанию в ArchRiot 3.4.

- Проект: https://github.com/paper-design/paper-mono/tree/main
- Файлы шрифтов включены под `config/fonts` и устанавливаются в `~/.local/share/fonts` во время установки.
- Пожалуйста, обратитесь к вышестоящему репозиторию для получения лицензии и информации об авторстве.

## Обновление

```
curl -fsSL https://ArchRiot.org/setup.sh | bash
```

# Примечания к выпуску ArchRiot 3.2

Сосредоточены на сохраняемости и полировке UX для управления громкостью, горячих клавиш и SSH ключей.

## Основные моменты

- Нет дублирующихся уведомлений о громкости
    - Аппаратные горячие клавиши XF86 аудио теперь вызывают Volume.sh с --no-notify, чтобы предотвратить двойные тосты
    - Визуальные элементы Waybar остаются без изменений (прокрутка/клик работают как раньше)
- Сохраняемые пользовательские горячие клавиши
    - Hyprland использует config/hypr/hyprland.conf напрямую по умолчанию (без отдельного keybindings.conf).
    - Установщик сохраняет этот файл (вместе с monitors.conf), так что ваши привязки переживают ремонт
- Сохраняемость оболочки и SSH
    - Советует помещать личные алиасы в ~/.config/fish/conf.d/local.fish (сохраняется установщиком)
    - Предоставляет ~/.config/fish/conf.d/ssh-agent.fish (не устанавливается по умолчанию). Чтобы включить: ln -s ~/.local/share/archriot/config/fish/conf.d/ssh-agent.fish ~/.config/fish/conf.d/ssh-agent.fish
- Единые примеры на весь экран
    - Задокументированные, необязательные привязки, которые вы можете включить в keybindings.conf:
        - bind = $mod, F11, fullscreen
        - bind = ALT, RETURN, fullscreen

## Обновление

```
curl -fsSL https://ArchRiot.org/setup.sh | bash
```

# Примечания к выпуску ArchRiot 3.1

Сосредоточены на стабильности и полировке UX, связанных с модулями Waybar, аудио и привязками приложений.

## Основные моменты

- Обнаружение температуры ЦП AMD для пользовательского модуля температуры Waybar (k10temp/zenpower), плюс Intel и общие запасные варианты
- Устойчивость аудио Waybar: модуль громкости благополучно снижается, когда pamixer/PipeWire не готовы, предотвращая сбои
- Аппаратные клавиши громкости в Hyprland: XF86AudioRaiseVolume, XF86AudioLowerVolume, XF86AudioMute, XF86AudioMicMute подключены к Volume.sh
- Лаунчер настроек клавиатуры теперь уважает $VISUAL/$EDITOR и возвращается к общим редакторам
- Лаунчер Bluesky (.desktop) и правила окон PWA для согласования опыта плавающей панели
- Определение модуля Waybar Mullvad присутствует; показывает отключено, когда Mullvad не установлен

## Обновление

```
curl -fsSL https://ArchRiot.org/setup.sh | bash
```

# Примечания к выпуску ArchRiot 3.0

Сосредоточены на надежности, переносимости и предсказуемом поведении на разнообразном железе — оставаясь верным философии ArchRiot: конфиденциальность прежде всего, ориентированность на разработчиков.

## Основные моменты

- Переносимость Waybar
    - Сетевой модуль больше не жестко кодирует имена интерфейсов; автоматически обнаруживает надежно.
    - Модуль температуры больше не жестко кодирует пути hwmon; автоматически обнаруживает датчики на разных железах.
    - Перезагрузка без мерцания через SIGUSR2 (без kill/restart).

- Индикатор записи экрана
    - Точка записи Kooha показана как крайний левый элемент справа; клик-стоп запись.
    - Чистое, легкое обнаружение на базе PipeWire; нулевой след в простое.

- Стек порталов выровнен с Hyprland
    - Добавлены xdg-desktop-portal, xdg-desktop-portal-hyprland, xdg-desktop-portal-gtk для согласованного поведения скринкаста/скриншота/выбора файла.

- Надежность аудио
    - rtkit добавлен для планирования реального времени PipeWire/WirePlumber под нагрузкой.

- Настройка памяти (opt-in)
    - При включении, vm.min_free_kbytes ≈ 1% of RAM (ограничено до 256MB) и эвристика перераспределения (vm.overcommit_memory=0), чтобы избежать голодания exec при сохранении отзывчивости.

- Автоопределение GPU
    - Устанавливает правильный драйвер NVIDIA/AMD/Intel и стек VA на основе обнаружения lspci.

- Полировка Hyprlock
    - Обновление метки ЦП и памяти снижено до 5s для снижения использования ЦП в фоновом режиме при блокировке.

- Fuzzel остается
    - Мы не используем Walker; Fuzzel остается лаунчером.

## Более безопасные значения по умолчанию

- TRIM отключен по умолчанию
    - Нет автоматического включения fstrim.timer (особенно важно для LUKS+btrfs). Если вы хотите еженедельный TRIM, включите `fstrim.timer` самостоятельно и рассмотрите возможность добавления `discard` в ваше LUKS-сопоставление (с обычными компромиссами конфиденциальности).

- Флаги VA-API Brave не принудительны
    - Мы не добавили агрессивные флаги браузера по умолчанию; если вы хотите аппаратное декодирование, проверьте с `vainfo` и настройте ваши флаги браузера локально по мере необходимости.

## Отложено на более позднее обновление

- Направляемый поток sbctl Secure Boot (сквозной).
- Необязательная интеграция systemd-oomd (opt-in с защищенными срезами).

## Обновление

Запустите one-liner:

```
curl -fsSL https://ArchRiot.org/setup.sh | bash
```

Спасибо за отзыв и тестирование, которые сформировали 3.0. Этот релиз сосредоточен на надежных значениях по умолчанию и переносимости на разном железе без ущерба для принципов ArchRiot.

## ✨ Что нового в v3.9.3

- Защита установщика:
    - резервное копирование/редактирование monitors.conf теперь пропускаются, когда Hyprland активен (hyprctl -j monitors succeeds).
    - Предотвращает сброс дисплеев и сбои Brave во время обновлений.
- Защита процессов (документация):
    - plan.md обновлен с непререкаемыми правилами, чтобы избежать перезагрузки композитора в потоках установщика, никогда не добавлять новые .sh для функций (используйте флаги Go), редактировать README только внизу и управлять включением/запуском модулей systemd в зависимости от существования модуля.
- Нет видимых пользователем изменений функций; это выпуск безопасности и надежности.

## ✨ Что нового в v3.9.4

- Восстановление при зависании порталов:
    - Новый флаг: `~/.local/share/archriot/install/archriot --portals-restart`
    - Безопасно перезапускает `xdg-desktop-portal-hyprland` и `xdg-desktop-portal` через systemd --user, когда доступно; переходит к лучшим усилиям kill+start.
- Необязательная привязка клавиши (предложение закомментировано):
    - `bind = $mod SHIFT, P, exec, $HOME/.local/share/archriot/install/archriot --portals-restart  # Restart portals`
- Нет перезагрузки композитора; это целевое улучшение надежности.

## ✨ Что нового в v3.9.5

- Безопасность конфигурации Hyprland во время обновлений:
    - Копирование конфигурации с учетом diff: пропускает запись, когда нет изменений (предотвращает автоматическую перезагрузку).
    - Никакого резервного копирования/sed monitors.conf во время установки (ноль лишней работы с дисплеями).
    - Всегда принудительно применять дисплеи после обновления конфигурации (без блокировки Brave).
- Стабильность во время редактирования:
    - автозапуск kanshi отключен внутри Hyprland, чтобы избежать переворотов вывода, вызванных перезагрузкой, во время редактирования.
- Нет перезагрузки композитора; это выпуск, ориентированный на надежность.
