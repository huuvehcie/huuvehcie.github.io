| № | Наименование проверки | Действие (шаги) | Ожидаемый результат | Ссылка на документацию |
|---|---|---|---|---|
| 1 | **Проверка создания и структуры табличного списка** | 1. В веб-интерфейсе перейти в раздел управления табличными списками.<br>2. Создать новый список, указав имя и структуру столбцов (например, `IP-адрес`, `Описание`). | Список создан и отображается в интерфейсе. Структура соответствует заданной. |  |
| 2 | **Проверка ручного заполнения списка** | 1. Открыть созданный список.<br>2. Добавить несколько записей вручную.<br>3. Сохранить изменения. | Записи успешно добавлены и отображаются в таблице. |  |
| 3 | **Проверка импорта данных из CSV-файла** | 1. Подготовить CSV-файл с тестовыми данными, соответствующими структуре списка.<br>2. В интерфейсе списка выполнить импорт файла, указав соответствие столбцов. | Данные из CSV-файла успешно импортированы. Записи отображаются корректно, без искажений. |  |
| 4 | **Проверка автоматического заполнения из правила корреляции (Enrichment)** | 1. Создать правило корреляции, в действии которого указать «Добавить в табличный список».<br>2. Сгенерировать событие, удовлетворяющее условиям правила.<br>3. Проверить содержимое списка. | В табличном списке автоматически появилась новая запись, соответствующая данным из события. |  |
| 5 | **Проверка использования списка в правиле корреляции (условие)** | 1. Создать правило корреляции с условием, использующим данные из табличного списка (например, `IP в списке`).<br>2. Сгенерировать тестовые события с IP-адресами из списка и вне его.<br>3. Проверить срабатывание правила. | Правило срабатывает только для событий, данные которых присутствуют в табличном списке. |  |
| 6 | **Проверка производительности и корректности хранения** | 1. Заполнить список большим количеством записей (например, >1000).<br>2. Проверить время отклика интерфейса и корректность сохранения данных. | Интерфейс работает без задержек (время отклика в пределах нормы). Данные сохраняются и отображаются корректно. |  |
