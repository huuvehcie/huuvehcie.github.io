# Firefox Developer Tools

Этот документ описывает актуальные вкладки инструментов разработчика Firefox (DevTools) по состоянию на последнюю документацию (MDN, Firefox Source Docs, 2025). Он предназначен для пентестеров и QA-инженеров, с акцентом на безопасность, тестирование и отладку веб-приложений. Включает подробные описания возможностей, примеры задач и технические пояснения.
---

2. [🖥️ Консоль (Console)](#консоль-console)
3. [🧬 Инспектор (Inspector)](#инспектор-inspector)
4. [🐞 Отладчик (Debugger)](#отладчик-debugger)
5. [🌐 Сеть (Network)](#сеть-network)
6. [🗃️ Хранилище (Storage)](#хранилище-storage)
7. [📈 Производительность (Performance)](#производительность-performance)
8. [🧠 Память (Memory)](#память-memory)
9. [♿ Доступность (Accessibility)](#доступность-accessibility)
10. [📱 Приложение (Application)](#приложение-application)
11. [🎬 Анимации (Animations)](#анимации-animations)
12. [📸 Скриншоты (Take Screenshot)](#скриншоты-take-screenshot)

---

## 🖥️ Консоль (Console)

📌 **Назначение**

Вкладка Консоль (Console) используется для просмотра, фильтрации и взаимодействия с сообщениями, которые выводятся в браузер в процессе загрузки и исполнения веб-страницы. Это основное средство диагностики JavaScript, проверки логики на стороне клиента, отслеживания ошибок, предупреждений, запросов в сеть, событий DOM и других активностей.

🔍 **В пентесте**

Позволяет:
- Перехватывать и выполнять JavaScript на странице;
- Тестировать XSS-инъекции;
- Обнаруживать чувствительные данные в сообщениях/ошибках;
- Выполнять атаки типа JavaScript Injection;
- Анализировать взаимодействия с API в реальном времени;
- Модифицировать контекст выполнения на клиентской стороне (в том числе удалять защиту на уровне JS).

🧪 **В QA**

Используется для:
- Отслеживания ошибок и предупреждений JavaScript;
- Проверки логов отлаживаемого приложения;
- Валидации корректности логирования;
- Фиксации событий, которые не видны в UI (ошибки загрузки, нарушения CORS, CSP);
- Проверки отработки обработчиков событий.

🧰 **Возможности (подробно)**

| Возможность | Подробное описание |
|-------------|--------------------|
| Просмотр ошибок JS | Автоматически выводятся ошибки и предупреждения JavaScript, включая стек вызова. |
| Логирование событий | console.log(), console.warn(), console.error() и др. доступны для просмотра и фильтрации. |
| Выполнение команд вручную | Полноценный REPL: можно выполнять JavaScript прямо в консоли в контексте текущей страницы. |
| Фильтрация сообщений | Можно фильтровать по типу сообщений (ошибки, предупреждения, логи, сеть, CSS). |
| Подсветка и навигация по коду | По клику на строку кода можно перейти к источнику в отладчике. |
| Использование глобальных переменных | Можно обращаться к переменным, функциям и DOM элементам прямо из консоли. |
| Просмотр сетевых запросов | Выводятся fetch(), XMLHttpRequest, WebSocket-события с возможностью анализа ответов. |
| Просмотр событий DOM | Можно логировать события (например, click, keydown) и проверять их обработку. |
| Интерполяция и шаблоны | console.log("ID: %d, user: %s", 123, "admin") — удобно для форматированного вывода. |
| Управление логгированием | Использование групп (console.group()) и счётчиков (console.count()) помогает структурировать логи. |
| Доступ к $0, $1 и др. | Ссылки на выделенные в инспекторе DOM-элементы (удобно для интерактивной отладки). |

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Выполнить alert(document.cookie) для теста XSS | Pentest: проверка уязвимости |
| 2 | Проверить, выводятся ли логи из console.log() | QA: логгирование |
| 3 | Выполнить document.forms[0].submit() и перехватить поведение формы | Pentest: обход защиты/ботчека |
| 4 | Найти ошибку "Uncaught TypeError: Cannot read property 'x' of undefined" | QA: анализ краша |
| 5 | Протестировать выполнение fetch('/api/user') вручную и посмотреть ответ | Pentest/QA: API-тестирование |
| 6 | Использовать debugger; внутри функции, чтобы остановить выполнение | QA: точка останова |
| 7 | Проверить, сработает ли вызов функции window.adminLogin() | Pentest: обход логики доступа |
| 8 | Ввести произвольный скрипт в поле и проверить реакцию | Pentest: проверка XSS |
| 9 | Удалить прослушиватель событий document.body.onclick = null | Pentest: снятие защиты |
| 10 | Проверить корректность шаблонизации при логировании | QA: локализация, ошибки форматирования |
| 11 | Проверить поведение DOM после вставки document.body.innerHTML += "<input>" | QA/Pentest: динамическая модификация |
| 12 | Сравнить значения переменной в разное время (console.log(token)) | QA: отладка сессий |
| 13 | Определить утечку ключей API в логах | Pentest: анализ безопасности |
| 14 | Проверить CORS ошибку при междоменном запросе | QA: конфигурация сервера |
| 15 | Получить список всех iframe на странице: document.querySelectorAll("iframe") | Pentest: анализ внешнего контента |

🛡️ **Особенности для безопасности**

| Цель пентестера | Используемый приём |
|-----------------|---------------------|
| Поиск XSS-инъекций | Ввод вредоносного скрипта и проверка реакции в консоли |
| Верификация client-side логики | Проверка JS-функций, проверка скрытых путей через вызовы функций вручную |
| Раскрытие токенов, ключей | Анализ значений переменных, логов, выводимых в консоль |
| Проверка обработки событий | Добавление/удаление слушателей, имитация событий (dispatchEvent) |
| Перехват запросов | Прямое выполнение fetch() для анализа поведения API |
| Ввод отладочных точек | Использование debugger;, чтобы приостановить выполнение и исследовать окружение |
| Обход UI-защиты | Принудительный вызов внутренних методов (.click(), .submit()), удаление атрибутов защиты |
| Проверка политики безопасности | Анализ CSP через ошибки в консоли, блокировку скриптов и ресурсов |
| Подмена данных в рантайме | Изменение содержимого переменных и функций |
| Обход DOM-защиты | Вставка новых элементов в DOM через JS |

⚙️ **Технические пояснения (для новичка)**

- JavaScript-код в консоли исполняется в контексте текущей вкладки, то есть имеет доступ к переменным, функциям, DOM.
- Консоль — это REPL (Read-Eval-Print Loop), т.е. можно вводить команду, она выполняется, и сразу виден результат.
- Консоль автоматически фильтрует типы сообщений, чтобы избежать "шума" — вы можете переключать фильтры вручную.
- При работе с DOM можно использовать специальные переменные:
  - `$0` — последний выбранный элемент в Инспекторе;
  - `$1`, `$2` — предыдущие элементы.
- Можно использовать многострочные команды, например:
  ```javascript
  {
    let items = document.querySelectorAll('a');
    for (let item of items) {
      console.log(item.href);
    }
  }
  ```
- Ошибки обычно имеют формат:
  ```
  Uncaught ReferenceError: variable is not defined at script.js:15
  ```
  → Это значит, что переменная не существует, и ошибка произошла в файле script.js на 15-й строке.

---

## 🧬 Инспектор (Inspector)

📌 **Назначение**

Инструмент «Инспектор» используется для анализа и изменения структуры HTML и стилей CSS прямо в браузере. Это основной инструмент для ручного аудита клиентской части веб-приложений, включая DOM, стили, layout, шрифты и кроссбраузерную совместимость.

🔍 **В пентесте**

Позволяет:
- Изучать скрытые или условно-отображаемые элементы DOM (например, скрытые поля, `display: none`).
- Обнаруживать клиентские проверки, не дублированные на сервере.
- Подменять значения в DOM для тестирования уязвимостей (например, внедрение скриптов).
- Изучать структуру одностраничных приложений (SPA), где маршрутизация управляется JavaScript.
- Анализировать шрифты и их источники для выявления сторонних ресурсов.
- Проверять кроссбраузерные уязвимости через панель Compatibility.

🧪 **В QA**

Используется для:
- Проверки, корректно ли применяются стили и классы при определённых действиях пользователя.
- Поиска и анализа багов визуального отображения.
- Проверки реакций UI на пользовательские сценарии (например, наведение мыши, фокус на поле, ошибки валидации).
- Быстрой отладки без пересборки проекта.
- Тестирования адаптивности через панель Layout.
- Валидации используемых шрифтов и их рендеринга.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Просмотр дерева DOM | Отображение иерархии элементов HTML-документа в реальном времени. |
| Живое редактирование DOM | Можно менять HTML прямо в браузере: вставлять/удалять элементы, редактировать текст и атрибуты. |
| Редактирование CSS | Просмотр и изменение применённых CSS-правил с подсказками, возможностью добавлять/удалять/переопределять стили. |
| Стили наследования и каскад | Показ всех стилей, включая переопределённые, с цветовой подсветкой источников (встроенные, подключённые файлы, user-agent). |
| Визуальные инструменты | Манипуляция box-model, перемещение и трансформация элементов с помощью UI. |
| Псевдоклассы и состояния | Принудительное применение `:hover`, `:focus`, `:active`, и других состояний к элементам. |
| Панель "Сетка" и "Контейнеры" | Анализ поведения flexbox и CSS grid в визуальной форме. |
| Подключённые события | Просмотр обработчиков событий (`onclick`, `onchange`, addEventListener и др.). |
| Привязка к элементу | Возможность выделить элемент на странице и сразу перейти к нему в DOM. |
| Панель "Шрифты" | Анализ используемых шрифтов, их источников и свойств (размер, вес, начертание). |
| Панель "Совместимость" | Проверка кроссбраузерных проблем CSS и HTML. |

📘 **Описание: как это работает**

- DOM (Document Object Model) — это представление HTML-документа в виде дерева объектов.
- Инспектор позволяет обращаться к каждому узлу этого дерева, изменять его, анализировать применённые стили и поведение.
- Все изменения производятся только в клиенте — в реальном DOM браузера. Они **не сохраняются** на сервере.
- Изменения в DOM мгновенно отображаются на странице, что удобно для тестирования изменений UI или атаки через модификацию клиентского поведения.
- Связь с CSS реализуется через вкладку стилей, где видны **все применённые правила**, включая переопределения и приоритеты (`!important`, специфичность).
- Панель Layout позволяет визуализировать flexbox, grid и другие контейнеры, а панель Fonts показывает информацию о подключённых шрифтах.
- Панель Compatibility (новая в последних версиях) выявляет проблемы совместимости с другими браузерами.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Область | Цель |
|---|-------|--------|------|
| 1 | Изменить скрытое поле input и отправить форму | Pentest | Тест клиентской валидации и доступности параметров |
| 2 | Вставить `<script>alert(1)</script>` в текстовое поле | Pentest | Проверка XSS уязвимостей |
| 3 | Принудительно установить `:hover` на кнопку | QA | Проверка стилизации в состоянии наведения |
| 4 | Изменить CSS класса `.btn-danger` | QA | Проверка визуальных изменений, связанных с цветами/оформлением |
| 5 | Удалить элемент `div.modal` | Pentest | Проверка поведения без оверлеев/попапов |
| 6 | Подменить текст `innerText` | QA | Проверка отображения многословного текста или разных языков |
| 7 | Добавить новый блок `<div>` | QA | Эмуляция новых компонентов на UI |
| 8 | Удалить обработчик события `onclick` | Pentest | Отключение защитных механизмов на кнопках |
| 9 | Проверить порядок наложения стилей | QA | Анализ каскадирования CSS |
| 10 | Изменить `readonly` на `input` | Pentest | Попытка ввести значения в защищённые поля |
| 11 | Активировать `:focus-visible` | QA | Проверка стилей доступности при навигации с клавиатуры |
| 12 | Инспектировать `iframe` | Pentest | Выявление изолированных контекстов/данных |
| 13 | Проверить параметры `data-*` | Pentest | Анализ скрытых данных, используемых JS |
| 14 | Подключить псевдокласс `:active` | QA | Проверка реакции UI при нажатии |
| 15 | Проверить подключённые шрифты и их источники | Pentest | Анализ сторонних ресурсов |

🛡️ **Особенности с точки зрения безопасности**

| Особенность | Значение для пентеста |
|-------------|-----------------------|
| Позволяет находить скрытые поля и данные в DOM | Возможность выявить параметры, которые не отображаются в UI, но участвуют в запросах |
| Отображает inline JavaScript | Обнаружение клиентской логики, проверок, токенов |
| Позволяет редактировать HTML | Возможность тестировать манипуляции, XSS, подмену содержимого |
| Доступен просмотр обработчиков событий | Упрощённый анализ client-side защиты (например, отключение `onsubmit`) |
| Видны параметры `data-*`, `hidden`, `aria-*` | Используются во многих кастомных реализациях бизнес-логики |
| Совместим с Content Security Policy (CSP) | Позволяет быстро увидеть, какие скрипты загружаются и какие блокируются |
| Можно инспектировать `iframe` (если разрешён CORS) | Проверка на clickjacking, перехват данных между доменами |
| Возможность просмотра кода SVG, canvas | Визуальный анализ сгенерированных графических объектов |
| Проверка кроссбраузерной совместимости | Выявление уязвимостей, связанных с различиями в рендеринге |

⚙️ **Технические пояснения (для новичка)**

- При наведении на элемент в DOM он подсвечивается на странице: это помогает соотнести разметку и визуальный результат.
- Стили отображаются в порядке применения: приоритет от более общих к более специфичным.
- Цветовая схема: синим — текущий стиль, зачёркнутый — переопределённый, серым — неактивный.
- Можно использовать командную строку (`Ctrl+Shift+C`), чтобы быстро выделить элемент на странице.
- Вкладки внутри «Инспектора»: **Rules (Стили)**, **Computed (Вычисленные стили)**, **Layout (Сетка)**, **Event Listeners (События)**, **Fonts (Шрифты)**, **Compatibility (Совместимость)**.
- Редактирование DOM не требует пересборки или обновления страницы: изменения сохраняются до обновления вручную.
- Панель Fonts показывает подключённые шрифты, их источники (локальные или внешние) и свойства.
- Панель Compatibility помогает выявить CSS-свойства, неподдерживаемые в других браузерах.

---

## 🐞 Отладчик (Debugger)

📌 **Назначение**

Вкладка "Отладчик" используется для анализа, пошагового выполнения и изучения JavaScript-кода, загруженного на страницу. Это основной инструмент для поиска логических ошибок, контроля состояния переменных и отслеживания работы динамически загружаемых скриптов.

🔍 **В пентесте**

Позволяет:
- Проводить обратную инженерию JavaScript, включая SPA и приложения с обфусцированным кодом.
- Обходить проверки клиента (например, антибот-логики, блокировки по IP).
- Отслеживать вызовы, ответственные за шаблоны авторизации или передачи токенов.
- Подменять переменные и функции на лету для анализа поведения защищённых участков интерфейса.

🧪 **В QA**

Используется для:
- Отладки ошибок в JavaScript, приводящих к падению функциональности.
- Поиска причин неработающих кликов, событий, AJAX-запросов.
- Проверки корректности логики валидации форм, переходов между страницами.
- Анализа правильности загрузки и инициализации скриптов (в нужном порядке и с нужными аргументами).

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Просмотр исходного кода JS, HTML, CSS, Web Workers | Можно открывать и навигировать между файлами. |
| Breakpoints (точки останова) | Останавливают выполнение кода на выбранной строке. |
| Watch Expressions | Наблюдение за переменными и выражениями в реальном времени. |
| Call Stack | Показывает стек вызовов функций. |
| Scope View | Переменные в разных контекстах (глобальный, локальный, замыкания). |
| Source Mapping | Отображение оригинального кода после минификации. |
| Live Edit | Можно изменять код прямо в браузере и видеть результат. |
| Conditional Breakpoints | Остановка при выполнении определенного условия. |
| XHR/Fetch Breakpoints | Остановка на запросах. |
| Event Listener Breakpoints | Остановка при срабатывании DOM-событий. |
| WebSocket Breakpoints | Для отладки соединений WS. |
| Пауза при исключениях | Останавливает выполнение при ошибках. |
| Debugger Statement | Поддержка `debugger;` из кода. |
| Интеграция с Console | Просмотр значений из console.log() в точках останова. |

📘 **Описание: как это работает**

Отладчик позволяет «заморозить» выполнение скриптов на нужной строке и шаг за шагом следить за значениями переменных, аргументами функций, контекстами исполнения. Это особенно полезно в асинхронных и событийно-ориентированных системах. Работает с минифицированным и сжатым кодом, поддерживает sourcemaps (карты соответствия между минифицированным и оригинальным кодом). Имеет встроенную панель переменных, точек останова, стека вызовов.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Остановить выполнение при клике на кнопку и отследить, какой обработчик вызывается | QA: отладка событий |
| 2 | Поставить breakpoint на login.js → submitLogin() и изучить, как формируются токены | Pentest: анализ авторизации |
| 3 | Подменить значение переменной user.isAdmin = true до отправки запроса | Pentest: обход прав |
| 4 | Найти, откуда вызывается sendBeacon() при выходе со страницы | QA: отладка аналитики |
| 5 | Проверить, вызывается ли checkCaptcha() при каждом сабмите формы | Pentest: обход защиты |
| 6 | Поставить условный breakpoint на cart.js, если price > 999 | QA: тестирование условий |
| 7 | Использовать XHR breakpoint, чтобы отследить вызов POST /api/checkout | Pentest/QA: API-тестирование |
| 8 | Посмотреть, какие данные попадают в WebSocket-сообщение | Pentest: анализ трафика |
| 9 | Найти ошибки try/catch, не приводящие к падению, но скрывающие проблему | QA: поиск багов |
| 10 | Проверить, меняется ли DOM внутри setTimeout в 5 секунд | QA: отладка асинхронности |
| 11 | Изменить код на лету: return false вместо return true в проверке прав | Pentest: манипуляция логикой |
| 12 | Понять, какие условия блокируют показ модального окна | QA: отладка UI |
| 13 | Разобраться, почему форма сбрасывается после отправки | QA: поиск багов |
| 14 | Выяснить, откуда вызывается повторная авторизация (refresh token) | Pentest: анализ сессий |
| 15 | Проверить правильность цепочки промисов при загрузке данных | QA: отладка асинхронности |

🛡️ **Особенности для безопасности**

| Объект | Уязвимость/Угроза | Последствие | Метод анализа |
|--------|-------------------|-------------|---------------|
| JavaScript авторизация | Проверка ролей на клиенте | Обход авторизации | Найти и подменить переменную user.role |
| Валидация формы | Только на клиенте | Bypass, XSS | Проверить validateForm() и изменить результат |
| WebSocket данные | Передаются без шифрования | Утечка данных | Перехватить и расшифровать payload |
| Проверка CAPTCHA | Можно отключить через отладчик | Бот может пройти | Поставить breakpoint и удалить вызов |
| Минифицированный JS | Скрывает логику | Сложнее анализ | Включить sourcemaps и отформатировать |
| try/catch с пустым блоком | Проглатывает ошибки | Уязвимость маскируется | Установить «Pause on exceptions» |
| eval()/new Function() | Может исполнять чужой код | RCE в клиенте | Отследить вызовы и аргументы |

⚙️ **Технические пояснения (для новичка)**

- **Точка останова (breakpoint)** — место в коде, где выполнение останавливается.
- **Step over / into / out** — команды пошаговой отладки (в пределах функции, вход внутрь, выход из функции).
- **Scope** — область видимости переменных (глобальный, локальный, внутри функции).
- **Call Stack** — последовательность вызовов функций до текущей точки.
- **Watch** — ручной список выражений, которые вы хотите отслеживать.
- **XHR Breakpoint** — отладка сетевых вызовов, сделанных через XMLHttpRequest или fetch.
- **Live Edit** — позволяет изменять код прямо в DevTools и сразу видеть эффект.

---

## 🌐 Сеть (Network)

📌 **Назначение**

Вкладка "Сеть" позволяет отслеживать весь сетевой трафик веб-страницы: HTTP/HTTPS-запросы, ответы, загружаемые ресурсы (CSS, JS, шрифты, изображения), WebSocket-сессии и другие подключения. Важный инструмент как для пентестера, так и для QA-инженера.

🔍 **В пентесте**

Позволяет:
- Анализировать перехватываемые данные (включая авторизационные токены, cookie, PII).
- Обнаруживать нешифрованные запросы.
- Искать скрытые API и эндпоинты (в том числе через XHR/fetch).
- Изучать CORS, CSRF, HSTS, CSP и другие заголовки безопасности.
- Захватывать и анализировать WebSocket, SSE, EventSource.

🧪 **В QA**

Используется для:
- Верификации правильности запросов и ответов (методы, коды состояния, payload).
- Отладки ошибок загрузки ресурсов (404, 500 и др.).
- Проверки кэширования, редиректов и времени загрузки.
- Анализа заголовков, куки, параметров и тел запросов/ответов.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Перехват всех сетевых запросов | В реальном времени: XHR, fetch, WebSocket, HTTP/2, SSE. |
| Фильтрация по типу запроса | HTML, CSS, JS, XHR, WS, Media, Fonts, Doc и пр. |
| Просмотр заголовков, тела запроса/ответа | Полная декомпозиция: Headers, Params, Cookies, Payload, Preview. |
| Копирование запроса в разных форматах | cURL, Fetch API, raw, JSON. |
| Повтор запроса (Replay / Resend) | Позволяет переотправить запрос с теми же параметрами. |
| Измерение времени загрузки | DNS, Connect, SSL, Wait, Receive (Waterfall-график). |
| WebSocket и SSE-инспекция | Возможность отслеживать сообщения по сессии. |
| Отображение всех редиректов | С деталями каждого шага. |
| Отображение состояния кэширования | Из заголовков и сетевого статуса. |
| CORS-индикация | Помогает быстро понять ошибки доступа. |
| Поддержка HAR-экспорта | Экспорт всех данных в HAR для анализа или баг-репортов. |

📘 **Описание: как это работает**

DevTools "встраивается" в сетевой стек браузера и отслеживает все исходящие и входящие сетевые запросы, включая REST, GraphQL, WebSocket, SSE и даже preflight-запросы (OPTIONS). Запросы отображаются в виде таблицы с колонками: URL, метод, статус, тип, размер и время. Работает через внутренние API Gecko и Netmonitor, взаимодействуя с HTTP Channel'ами браузера.

💡 **Советы**:
- Нажмите правой кнопкой мыши по запросу → Копировать как cURL — полезно для перетеста через терминал.
- Можно выбрать любой запрос и вручную изменить параметры → Редактировать и повторно отправить (Edit and Resend).
- WebSocket отображаются отдельным потоком: сообщения, направление, данные, флаги.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Проверить, уходит ли пароль в открытом виде через HTTP | Pentest: анализ шифрования |
| 2 | Отследить утечку токена в заголовке Authorization | Pentest: поиск утечек |
| 3 | Найти скрытые API-запросы при навигации SPA | Pentest: обнаружение эндпоинтов |
| 4 | Выявить Set-Cookie с флагами HttpOnly, Secure, SameSite | Pentest: проверка безопасности |
| 5 | Определить причину ошибки CORS | QA: отладка конфигурации |
| 6 | Перехватить и переотправить POST-запрос с изменённым телом | Pentest: тестирование API |
| 7 | Проверить кеширование запросов и ответов (заголовки Cache-Control, ETag) | QA: оптимизация загрузки |
| 8 | Оценить задержку первого байта (TTFB) | QA: анализ производительности |
| 9 | Изучить, где выполняется редирект (302) и по каким причинам | QA: отладка навигации |
| 10 | Проанализировать передачу токенов авторизации при обновлении сессии | Pentest: анализ сессий |
| 11 | Найти OPTIONS-preflight перед PUT/DELETE и отследить CORS-политику | QA: проверка CORS |
| 12 | Проверить политику безопасности через Content-Security-Policy, X-Frame-Options | Pentest: анализ заголовков |
| 13 | Выявить WebSocket-подключения и изучить их payload | Pentest: анализ трафика |
| 14 | Проверить, передаются ли заголовки Referer и Origin при переходах | Pentest: утечка данных |
| 15 | Сравнить поведение сайта при входе и без авторизации (разные ответы/права) | QA: тестирование прав |

🛡️ **Особенности для безопасности**

| Особенность | Применение в пентесте | Возможная угроза или защита |
|-------------|-----------------------|-----------------------------|
| Заголовок Authorization | Отслеживание токена | Возможна утечка в Referer |
| Куки без HttpOnly | XSS может украсть cookie | Флаг HttpOnly защищает от JS |
| Отсутствие SameSite | Уязвимость CSRF | Нужно использовать SameSite=Strict |
| WebSocket без шифрования | Перехват трафика | Должен использовать WSS |
| CORS с * или null | Может раскрывать данные | Проверка origin'ов обязательна |
| Прозрачные редиректы | Возможность open redirect | Нужно валидировать redirect-URL |
| Наличие заголовков безопасности | CSP, X-XSS-Protection, X-Content-Type-Options | Минимизируют риски RCE |
| JSONP, невалидный Content-Type | Обход SOP, XSS | Проверка Content-Type: application/json |
| Скрытые эндпоинты API | Показатель уровня экспонирования | Нужно применять rate-limit, auth |
| HAR-файлы | Могут содержать конфиденциальные данные | Не выгружать в открытые системы |

⚙️ **Технические пояснения (для новичка)**

- **HTTP-методы**: GET — получить данные, POST — отправить, PUT — заменить, DELETE — удалить.
- **Статус-коды**: 200 OK, 301/302 Redirect, 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 500 Server Error.
- **Заголовки запроса**: несут информацию о типе клиента, токенах, предпочтениях.
- **Заголовки ответа**: задают политику безопасности, кэш, авторизацию.
- **Preflight-запрос**: автоматический запрос методом OPTIONS, который проверяет разрешения CORS.
- **Waterfall**: диаграмма времён, показывающая последовательность сетевых событий.
- **WebSocket**: двунаправленное соединение между клиентом и сервером — всегда проверяй ws:// vs wss://.
- **HAR-файл**: лог сетевого трафика, пригодный для загрузки в анализаторы.

---

## 🗃️ Хранилище (Storage)

📌 **Назначение**

Вкладка "Хранилище" позволяет просматривать и анализировать все типы клиентского хранения данных в браузере: cookie, localStorage, sessionStorage, IndexedDB, кеши (Cache API), файлы и даже service workers. Она незаменима для пентестера при анализе клиентской стороны безопасности, и для QA-инженера — при тестировании функционала, связанного с состоянием и сессиями.

🔍 **В пентесте**

Позволяет:
- Выявлять инъекции XSS через localStorage и IndexedDB.
- Анализировать несанкционированные данные в cookie (особенно auth-токенов).
- Проверять флаги безопасности cookie (HttpOnly, Secure, SameSite).
- Выявлять нешифрованную PII, JWT или сессионную информацию.
- Изучать персистентные данные, уязвимые к Man-in-the-Browser атакам.

🧪 **В QA**

Используется для:
- Проверки, сохраняется ли состояние корректно между сессиями.
- Валидации работы авторизации через cookie и JWT через localStorage.
- Отладки работы сессионного кэша (например, если сброс состояния некорректен).
- Проверки реакции интерфейса на удаление/изменение хранимых значений.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Просмотр и редактирование cookie | Включая флаги безопасности и значения. |
| Анализ localStorage и sessionStorage | Ключи/значения в реальном времени. |
| Работа с IndexedDB | Структурированное хранилище объектов. |
| Доступ к Cache Storage | Изучение кэшированных запросов (PWA). |
| Работа с service workers | Отображение зарегистрированных SW и их статуса. |
| Редактирование и удаление данных | Вручную или через интерфейс. |
| Поддержка нескольких доменов | В том числе поддоменов и разных storage-контекстов. |
| Быстрый поиск по значениям | Особенно полезен при JWT и токенах. |
| Отслеживание изменений в реальном времени | Без перезагрузки страницы. |

📘 **Описание: как это работает**

Браузер предоставляет несколько уровней хранения:
- **cookie**: используется для авторизации, аналитики, предпочтений.
- **localStorage**: хранит данные без срока действия.
- **sessionStorage**: очищается при закрытии вкладки.
- **IndexedDB**: объектное хранилище для больших объемов данных.
- **Cache API**: используется в PWA для хранения офлайн-контента.
- **Service Workers**: отдельные фоновые процессы, которые могут обслуживать запросы.

DevTools позволяет просматривать, модифицировать или удалять данные в этих хранилищах, а также анализировать безопасность и состав информации.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Проверить, используется ли токен авторизации в localStorage (и можно ли его украсть через XSS) | Pentest: поиск утечек |
| 2 | Найти нешифрованные email/телефоны/PII в IndexedDB | Pentest: анализ данных |
| 3 | Изучить, сохраняется ли JWT и как долго он валиден | Pentest: анализ сессий |
| 4 | Проверить, устанавливаются ли cookie с флагами Secure и HttpOnly | Pentest: проверка безопасности |
| 5 | Удалить sessionStorage и посмотреть, как повлияет на поведение страницы | QA: отладка состояния |
| 6 | Отследить работу service worker и перехват запросов через него | Pentest: анализ PWA |
| 7 | Удалить ключ cartItems из localStorage и проверить реакцию сайта | QA: тестирование корзины |
| 8 | Изменить cookie role=admin и проверить авторизацию | Pentest: обход прав |
| 9 | Посмотреть, создается ли кэш при офлайн-загрузке (PWA) | QA: тестирование PWA |
| 10 | Найти устаревшие или “зависшие” значения, не очищаемые UI | QA: отладка багов |
| 11 | Найти IndexedDB базы, созданные сторонними скриптами | Pentest: анализ трекеров |
| 12 | Проверить, создается ли кэш при переходах (SPA) | QA: тестирование навигации |
| 13 | Удалить cookie сессии и попытаться взаимодействовать с защищённой страницей | Pentest: тестирование сессий |
| 14 | Изучить способ хранения пользовательских предпочтений | QA: тестирование UX |
| 15 | Провалидировать, что sensitive data не кэшируется в localStorage | Pentest: проверка безопасности |

🛡️ **Особенности для безопасности**

| Объект | Уязвимость | Последствие | Митигирующие меры |
|--------|------------|-------------|-------------------|
| Cookie без HttpOnly | Доступ через XSS | Кража сессии | Установить HttpOnly |
| Cookie без Secure | Передача по HTTP | Перехват в MITM | Включить HTTPS и Secure |
| localStorage с токеном | XSS → утечка | Подмена пользователя | Перевод в cookie с HttpOnly |
| sessionStorage | Уязвим к MITB | Угон сессии в браузере | Изоляция в sandbox |
| IndexedDB | Хранение чувствительных данных | Сложнее обнаружить, но опасно | Шифрование, периодическая очистка |
| Cache API | Кэширование секретов | Утечка офлайн | Правильные headers и cache control |
| Service Worker | Возможность перехвата/подмены | Man-in-the-Browser | CSP, рег. только по HTTPS |

⚙️ **Технические пояснения (для новичка)**

- **Cookie** — пара ключ=значение, автоматически отправляется с каждым запросом.
- **HttpOnly** — запрещает доступ к cookie из JS (важно для защиты от XSS).
- **Secure** — cookie будет передано только по HTTPS.
- **SameSite** — ограничивает передачу cookie между сайтами (Strict, Lax, None).
- **localStorage** — сохраняется до очистки вручную.
- **sessionStorage** — хранится только в текущей вкладке.
- **IndexedDB** — база данных объектов, хранится локально, доступна через JS API.
- **Cache Storage** — часть Service Worker API, позволяет сохранять запросы.
- **Service Worker** — скрипт, работающий в фоне, может перехватывать запросы, кешировать ресурсы.

---

## 📈 Производительность (Performance)

📌 **Назначение**

Вкладка "Производительность" предназначена для анализа производительности веб-страницы, включая временные характеристики её загрузки, рендеринга, выполнения JavaScript и взаимодействия с пользователем. Это инструмент для выявления узких мест, оптимизации скорости и обеспечения стабильности приложения.

🔍 **В пентесте**

Позволяет:
- Анализировать время отклика сервера для выявления уязвимостей к DDoS или неоптимизированных эндпоинтов.
- Отслеживать выполнение JavaScript для поиска уязвимостей (например, чрезмерное потребление ресурсов).
- Проверять производительность при нагрузочных атаках или попытках эксплуатации.

🧪 **В QA**

Используется для:
- Оптимизации времени загрузки страницы и её компонентов.
- Проверки производительности при различных сценариях (медленный интернет, слабое устройство).
- Анализа влияния сторонних скриптов, API и динамического контента на UX.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Запись профиля производительности | Создание временной шкалы событий (загрузка, рендеринг, JS, отрисовка). |
| Визуализация Waterfall | График, показывающий последовательность и длительность событий. |
| Анализ выполнения JavaScript | Детализация времени, затраченного на функции и события. |
| Flame Chart | Визуализация стека вызовов JS с указанием узких мест. |
| Проверка рендеринга | Анализ частоты кадров (FPS), времени отрисовки и layout shifts. |
| События DOMContentLoaded и Load | Отслеживание ключевых этапов загрузки страницы. |
| Экспорт профиля | Сохранение данных в JSON для дальнейшего анализа. |
| Симуляция условий | Эмуляция медленного интернета или слабого CPU. |

📘 **Описание: как это работает**

Вкладка "Производительность" записывает временную шкалу всех событий страницы: от загрузки ресурсов до выполнения скриптов и рендеринга. Результаты отображаются в виде графиков (Waterfall, Flame Chart), позволяя выявить долгие запросы, блокировки основного потока или неэффективный код. Работает через Performance API браузера и внутренние инструменты Gecko.

💡 **Советы**:
- Используйте кнопку "Start Recording" для захвата профиля.
- Анализируйте Flame Chart для поиска долгих функций JS.
- Симулируйте 3G или слабый CPU через настройки DevTools.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Определить ресурсы, загружаемые дольше всего (изображения, JS) | QA: оптимизация загрузки |
| 2 | Выявить время выполнения JavaScript для поиска узких мест | QA: оптимизация скриптов |
| 3 | Проверить, блокирует ли крупный ресурс загрузку других | QA: анализ Waterfall |
| 4 | Проанализировать время рендеринга DOM | QA: тестирование UX |
| 5 | Оценить TTFB при слабом интернете | QA: тестирование условий |
| 6 | Проверить влияние рекламы на производительность | QA: анализ сторонних скриптов |
| 7 | Измерить скорость отклика API | QA: тестирование бэкенда |
| 8 | Проанализировать загрузку AJAX-контента | QA: тестирование SPA |
| 9 | Тестировать производительность на мобильных устройствах | QA: кросс-девайс тестирование |
| 10 | Сравнить отклик сервера до и после оптимизации | QA: анализ изменений |
| 11 | Проверить время до первого рендера | QA: тестирование UX |
| 12 | Исследовать задержки от веб-шрифтов | QA: оптимизация ресурсов |
| 13 | Анализировать производительность при множественных запросах | Pentest: нагрузочное тестирование |
| 14 | Выявить причины замедления после добавления контента | QA: отладка багов |
| 15 | Проверить работу при высоких нагрузках | Pentest: стресс-тестирование |

🛡️ **Особенности для безопасности**

| Объект | Уязвимость/Угроза | Последствие | Метод анализа |
|--------|-------------------|-------------|---------------|
| Долгие запросы на сервер | Слабая защита от DDoS | Потеря производительности | Проверка времени отклика на нагрузку |
| Невалидированные данные в запросах | Инъекции, утечки | Атаки через API | Тестирование обработки данных |
| Медленная загрузка страниц | Неоптимизированные ресурсы | Возможность DoS | Анализ загрузки ресурсов |
| Сторонние скрипты | Внедрение вредоносного кода | Уязвимости в библиотеках | Проверка источников скриптов |

⚙️ **Технические пояснения (для новичка)**

- **Рендеринг** — процесс отображения страницы, включающий загрузку ресурсов и построение DOM.
- **JavaScript** — однопоточный язык, где долгие скрипты могут блокировать рендеринг.
- **Waterfall** — график, показывающий последовательность событий (DNS, SSL, загрузка).
- **Flame Chart** — визуализация стека вызовов JS, где ширина блока отражает время выполнения.
- **DOMContentLoaded** — событие, сигнализирующее о завершении загрузки HTML.
- **Load** — событие полной загрузки всех ресурсов.
- **TTFB (Time to First Byte)** — время до получения первого байта ответа сервера.

---

## 🧠 Память (Memory)

📌 **Назначение**

Вкладка "Память" позволяет анализировать использование памяти веб-страницей, включая кучу (heap), объекты и ресурсы. Она помогает выявить утечки памяти, оптимизировать производительность и обеспечить стабильность приложения при длительной работе.

🔍 **В пентесте**

Позволяет:
- Выявлять утечки памяти, которые могут быть использованы для атак (например, исчерпание ресурсов).
- Анализировать подозрительные операции с памятью, связанные с вредоносными скриптами.
- Проверять влияние сторонних библиотек на потребление памяти.

🧪 **В QA**

Используется для:
- Обнаружения утечек памяти, приводящих к сбоям при длительных сессиях.
- Оптимизации потребления памяти для повышения производительности.
- Тестирования работы с большими данными или динамическим контентом.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Снимки кучи (Heap Snapshots) | Захват текущего состояния памяти для анализа объектов. |
| Отслеживание объектов | Просмотр всех объектов в памяти и их связей. |
| Анализ утечек памяти | Выявление объектов, не освобождаемых после использования. |
| Профилирование выделения памяти | Отслеживание выделения памяти во времени (Allocation Timeline). |
| Tree Map | Визуализация распределения памяти по типам объектов. |
| Dominators View | Показывает, какие объекты удерживают другие в памяти. |
| Comparison View | Сравнение двух снимков для выявления изменений. |

📘 **Описание: как это работает**

Вкладка "Память" использует Performance API и Heap Profiler для анализа кучи JavaScript. Снимки кучи показывают все объекты, их размер и зависимости. Профилирование выделения отслеживает создание и удаление объектов в реальном времени. Инструмент помогает найти утечки памяти, когда объекты остаются в памяти из-за неверных ссылок.

💡 **Советы**:
- Делайте несколько снимков в разных состояниях приложения.
- Используйте Comparison View для поиска новых объектов.
- Анализируйте Dominators View для выявления "удерживающих" объектов.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Проанализировать использование памяти на странице | QA: оптимизация ресурсов |
| 2 | Выявить объекты, занимающие слишком много памяти | QA: оптимизация объектов |
| 3 | Проверить, остаются ли объекты после закрытия модального окна | QA: поиск утечек |
| 4 | Обнаружить утечку памяти при добавлении DOM-элементов | QA: отладка багов |
| 5 | Проверить рост памяти при длительных сессиях | QA: тестирование стабильности |
| 6 | Оценить, как долго страница работает без утечек | QA: стресс-тестирование |
| 7 | Измерить влияние AJAX на память | QA: тестирование динамики |
| 8 | Проверить утечки от сторонних скриптов | Pentest: анализ трекеров |
| 9 | Профилировать объекты для поиска зависимостей | QA: оптимизация кода |
| 10 | Оценить влияние iframe на память | QA: тестирование встраиваний |
| 11 | Проверить API-запросы на утечки памяти | QA: отладка асинхронности |
| 12 | Сравнить память до и после изменений кода | QA: анализ оптимизации |
| 13 | Выявить проблемы с удалением объектов | QA: поиск багов |
| 14 | Анализировать память при работе с большими таблицами | QA: тестирование данных |
| 15 | Выявить утечки при обработке событий (клики, скролл) | QA: отладка событий |

🛡️ **Особенности для безопасности**

| Объект | Уязвимость/Угроза | Последствие | Метод анализа |
|--------|-------------------|-------------|---------------|
| Утечка памяти | Эксплуатация для исчерпания ресурсов | DoS-атаки | Снимки и профилирование памяти |
| Сторонние скрипты | Вредоносный код | Утечки или атаки | Анализ источников скриптов |
| Большие данные | Неэффективное управление | Сбои и утечки | Профилирование больших данных |
| Ошибки кода | Неправильные ссылки | Уязвимости памяти | Анализ снимков и зависимостей |

⚙️ **Технические пояснения (для новичка)**

- **Утечка памяти** — объекты, остающиеся в памяти после ненадобности.
- **Куча (Heap)** — область памяти для динамически выделяемых объектов.
- **Снимок кучи** — моментальное состояние памяти с объектами и их связями.
- **Профилирование** — отслеживание выделения памяти во времени.
- **Dominators** — объекты, удерживающие другие в памяти через ссылки.
- **Tree Map** — визуализация, где размер блока отражает объем памяти.

---

## ♿ Доступность (Accessibility)

📌 **Назначение**

Вкладка "Доступность" предназначена для проверки соответствия веб-страницы стандартам доступности (WCAG, ARIA). Она помогает выявить проблемы, мешающие пользователям с ограниченными возможностями взаимодействовать с сайтом, и обеспечивает соответствие законодательным требованиям.

🔍 **В пентесте**

Позволяет:
- Проверять отсутствие ARIA-атрибутов, что может скрывать уязвимости в UI.
- Анализировать доступность скрытых элементов, которые могут быть использованы для атак.
- Выявлять невалидные ARIA-роли, приводящие к некорректной обработке.

🧪 **В QA**

Используется для:
- Проверки читаемости сайта для экранных читалок.
- Тестирования навигации с клавиатуры и фокуса.
- Валидации ARIA-атрибутов и семантической разметки.
- Обеспечения доступности для пользователей с нарушениями зрения, слуха или моторики.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Accessibility Tree | Отображение дерева доступности, показывающего, как элементы интерпретируются assistive технологиями. |
| Проверка ARIA-атрибутов | Анализ корректности ролей, состояний и свойств ARIA. |
| Тестирование фокуса | Проверка навигации с клавиатуры и управления фокусом. |
| Контрастность | Анализ цветового контраста для соответствия WCAG. |
| Инспекция элементов | Показывает, как элементы интерпретируются экранными читалками. |
| Симуляция нарушений зрения | Эмуляция дальтонизма или низкого зрения для тестирования UX. |
| Отчеты об ошибках | Список проблем доступности с рекомендациями по исправлению. |

📘 **Описание: как это работает**

Вкладка "Доступность" анализирует DOM и ARIA-атрибуты, строя Accessibility Tree — представление страницы для assistive технологий (экранных читалок, брайлевских дисплеев). Она проверяет соответствие стандартам WCAG, фокус, контрастность и семантику, помогая улучшить UX для всех пользователей.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Проверить, читаются ли элементы экранной читалкой | QA: тестирование доступности |
| 2 | Найти элементы без ARIA-ролей | QA: валидация семантики |
| 3 | Проверить навигацию с клавиатуры | QA: тестирование UX |
| 4 | Анализировать контрастность кнопок и текста | QA: соответствие WCAG |
| 5 | Выявить скрытые элементы, доступные читалкам | Pentest: анализ скрытых данных |
| 6 | Проверить ARIA-атрибуты на формах | QA: тестирование форм |
| 7 | Симулировать дальтонизм для проверки UX | QA: тестирование восприятия |
| 8 | Найти невалидные ARIA-роли | QA: валидация разметки |
| 9 | Проверить фокус на модальных окнах | QA: тестирование UX |
| 10 | Анализировать доступность динамического контента | QA: тестирование SPA |
| 11 | Проверить, не раскрывают ли ARIA-атрибуты конфиденциальные данные | Pentest: утечка данных |
| 12 | Тестировать озвучивание ошибок валидации | QA: тестирование форм |
| 13 | Проверить доступность iframe-контента | QA: тестирование встраиваний |
| 14 | Анализировать семантику таблиц | QA: валидация разметки |
| 15 | Проверить, блокируются ли элементы без фокуса | Pentest: обход UI |

🛡️ **Особенности для безопасности**

| Объект | Уязвимость/Угроза | Последствие | Метод анализа |
|--------|-------------------|-------------|---------------|
| Неверные ARIA-атрибуты | Неправильная интерпретация UI | Утечка данных через читалки | Проверка ARIA-ролей |
| Скрытые элементы | Доступны через assistive технологии | Раскрытие конфиденциальных данных | Анализ Accessibility Tree |
| Отсутствие фокуса | Обход UI-защиты | Некорректное взаимодействие | Тестирование навигации |
| Низкая контрастность | Уязвимости UX | Плохая читаемость | Проверка контраста |

⚙️ **Технические пояснения (для новичка)**

- **Accessibility Tree** — структура, показывающая, как страница интерпретируется assistive технологиями.
- **ARIA (Accessible Rich Internet Applications)** — атрибуты для улучшения доступности динамического контента.
- **WCAG (Web Content Accessibility Guidelines)** — международные стандарты доступности.
- **Фокус** — возможность управлять страницей с клавиатуры (Tab, Enter).
- **Контрастность** — соотношение яркости текста и фона для читаемости.

---

## 📱 Приложение (Application)

📌 **Назначение**

Вкладка "Приложение" предназначена для анализа и отладки прогрессивных веб-приложений (PWA), включая манифесты, service workers и кэшированные ресурсы. Она помогает тестировать офлайн-функционал и проверять конфигурацию PWA.

🔍 **В пентесте**

Позволяет:
- Анализировать service workers на предмет перехвата или подмены запросов.
- Проверять манифесты на наличие некорректных или уязвимых настроек.
- Выявлять кэшированные данные, которые могут содержать PII.

🧪 **В QA**

Используется для:
- Проверки корректности манифеста PWA (иконки, theme color, scope).
- Тестирования работы service workers в офлайн-режиме.
- Валидации кэширования ресурсов для быстрой загрузки.
- Отладки уведомлений push.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Анализ манифеста | Просмотр и валидация web app manifest (JSON). |
| Управление service workers | Регистрация, обновление, удаление SW. |
| Просмотр Cache Storage | Анализ кэшированных ресурсов PWA. |
| Тестирование push-уведомлений | Симуляция и отладка уведомлений. |
| Инспекция Storage | Доступ к cookie, localStorage, IndexedDB в контексте PWA. |
| Офлайн-режим | Симуляция работы без интернета. |

📘 **Описание: как это работает**

Вкладка "Приложение" предоставляет интерфейс для работы с PWA, включая манифест (определяет метаданные приложения), service workers (фоновые скрипты для офлайн-функций) и кэши. Она интегрируется с вкладкой "Хранилище" для анализа данных.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Проверить корректность манифеста PWA | QA: валидация конфигурации |
| 2 | Тестировать работу service worker в офлайн-режиме | QA: тестирование PWA |
| 3 | Анализировать кэшированные ресурсы | QA: оптимизация загрузки |
| 4 | Симулировать push-уведомления | QA: тестирование уведомлений |
| 5 | Проверить, перехватывает ли service worker запросы | Pentest: анализ безопасности |
| 6 | Найти PII в кэшированных данных | Pentest: утечка данных |
| 7 | Проверить scope манифеста | QA: валидация области |
| 8 | Удалить service worker и проверить поведение | QA: отладка багов |
| 9 | Анализировать HTTPS-регистрацию service worker | Pentest: проверка безопасности |
| 10 | Проверить иконки в манифесте | QA: тестирование UX |
| 11 | Тестировать работу PWA на мобильных устройствах | QA: кросс-девайс тестирование |
| 12 | Проверить, сохраняются ли данные в IndexedDB | QA: тестирование хранения |
| 13 | Анализировать ошибки манифеста | QA: отладка конфигурации |
| 14 | Проверить CSP для service workers | Pentest: анализ политики |
| 15 | Тестировать восстановление кэша после сбоя | QA: тестирование стабильности |

🛡️ **Особенности для безопасности**

| Объект | Уязвимость/Угроза | Последствие | Метод анализа |
|--------|-------------------|-------------|---------------|
| Service Worker | Перехват запросов | Man-in-the-Browser | Анализ кода SW |
| Манифест | Некорректный scope | Уязвимости доступа | Проверка JSON |
| Кэшированные данные | Хранение PII | Утечка данных | Анализ Cache Storage |
| Push-уведомления | Фишинг | Вредоносные уведомления | Тестирование payload |

⚙️ **Технические пояснения (для новичка)**

- **PWA** — веб-приложение, работающее как нативное (офлайн, уведомления).
- **Манифест** — JSON-файл с метаданными PWA (имя, иконки, theme).
- **Service Worker** — фоновый скрипт для кэширования и перехвата запросов.
- **Cache Storage** — хранилище для офлайн-ресурсов.
- **Push API** — механизм доставки уведомлений.

---

## 🎬 Анимации (Animations)

📌 **Назначение**

Вкладка "Анимации" предназначена для анализа и отладки CSS-анимаций и переходов на странице. Она помогает оптимизировать анимации, проверять их производительность и выявлять проблемы рендеринга.

🔍 **В пентесте**

Позволяет:
- Проверять анимации на наличие скрытых элементов, раскрывающих данные.
- Анализировать производительность анимаций для выявления уязвимостей DoS.
- Выявлять невалидные CSS-свойства, приводящие к ошибкам рендеринга.

🧪 **В QA**

Используется для:
- Проверки корректности CSS-анимаций и переходов.
- Тестирования производительности анимаций на слабых устройствах.
- Валидации UX при взаимодействии с анимированными элементами.
- Отладки багов, связанных с рендерингом анимаций.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Просмотр активных анимаций | Список всех CSS-анимаций и переходов на странице. |
| Временная шкала | График длительности и последовательности анимаций. |
| Изменение скорости | Замедление или ускорение анимаций для отладки. |
| Пауза анимаций | Остановка всех анимаций для анализа. |
| Инспекция свойств | Просмотр CSS-свойств (`animation`, `transition`) и их значений. |
| Проверка производительности | Анализ влияния анимаций на FPS и рендеринг. |

📘 **Описание: как это работает**

Вкладка "Анимации" интегрируется с CSS Animation API и рендеринговым движком Gecko. Она отображает все активные анимации, их свойства (длительность, задержка, easing) и временную шкалу, позволяя замедлять или останавливать их для анализа.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Проверить корректность анимации кнопки при наведении | QA: тестирование UX |
| 2 | Замедлить анимацию для анализа багов | QA: отладка рендеринга |
| 3 | Проверить влияние анимаций на FPS | QA: оптимизация производительности |
| 4 | Найти скрытые элементы, появляющиеся в анимации | Pentest: утечка данных |
| 5 | Тестировать анимации на мобильных устройствах | QA: кросс-девайс тестирование |
| 6 | Проверить, не блокируют ли анимации основной поток | QA: оптимизация рендеринга |
| 7 | Анализировать переходы в модальном окне | QA: тестирование UX |
| 8 | Проверить CSS-свойства анимации | QA: валидация стилей |
| 9 | Остановить анимации для проверки состояния | QA: отладка багов |
| 10 | Анализировать влияние сторонних анимаций | QA: тестирование библиотек |
| 11 | Проверить, не раскрывают ли анимации PII | Pentest: утечка данных |
| 12 | Тестировать easing-функции анимаций | QA: тестирование UX |
| 13 | Проверить анимации при слабом CPU | QA: оптимизация производительности |
| 14 | Анализировать ошибки в transition-property | QA: отладка стилей |
| 15 | Проверить CSP на блокировку анимаций | Pentest: анализ политики |

🛡️ **Особенности для безопасности**

| Объект | Уязвимость/Угроза | Последствие | Метод анализа |
|--------|-------------------|-------------|---------------|
| Скрытые элементы в анимации | Раскрытие данных | Утечка PII | Проверка временной шкалы |
| Тяжелые анимации | Потребление ресурсов | DoS-атаки | Анализ FPS |
| Неверные CSS-свойства | Ошибки рендеринга | Уязвимости UI | Проверка свойств |
| Сторонние анимации | Вредоносный код | Атаки через библиотеки | Анализ источников |

⚙️ **Технические пояснения (для новичка)**

- **CSS Animation** — анимации, заданные через `@keyframes` и свойство `animation`.
- **Transition** — плавные изменения CSS-свойств (например, `color`, `opacity`).
- **Easing** — функция, определяющая скорость анимации (linear, ease-in-out).
- **FPS (Frames Per Second)** — частота кадров, влияющая на плавность анимаций.
- **Рендеринг** — процесс отрисовки страницы, где анимации могут блокировать основной поток.

---

## 📸 Скриншоты (Take Screenshot)

📌 **Назначение**

Функция "Скриншоты" (не вкладка) позволяет делать снимки экрана всей страницы, видимой области или отдельного элемента. Она доступна через контекстное меню DevTools или команду (`Ctrl+Shift+S`).

🔍 **В пентесте**

Позволяет:
- Фиксировать скрытые элементы или ошибки UI для анализа.
- Документировать уязвимости (например, XSS или утечки данных).
- Сравнивать рендеринг страницы на разных этапах атаки.

🧪 **В QA**

Используется для:
- Документирования багов визуального отображения.
- Проверки адаптивности дизайна на разных разрешениях.
- Сравнения рендеринга до и после изменений.

🧰 **Возможности**

| Возможность | Описание |
|-------------|----------|
| Скриншот всей страницы | Сохранение полного контента, включая невидимую область. |
| Скриншот видимой области | Захват текущего viewport. |
| Скриншот элемента | Снимок выбранного DOM-элемента. |
| Копирование в буфер | Быстрое копирование изображения. |
| Сохранение в файл | Экспорт в PNG или JPEG. |

📘 **Описание: как это работает**

Функция использует рендеринговый движок Gecko для захвата изображения страницы или её части. Скриншоты сохраняются в высоком разрешении и могут включать весь контент, даже за пределами видимой области.

✅ **Примеры задач (пентест и QA)**

| № | Задача | Контекст применения |
|---|--------|---------------------|
| 1 | Сделать скриншот XSS-атаки | Pentest: документирование |
| 2 | Сравнить рендеринг до и после фикса бага | QA: тестирование UI |
| 3 | Захватить модальное окно для баг-репорта | QA: документирование |
| 4 | Проверить адаптивность на 320px | QA: тестирование дизайна |
| 5 | Сделать скриншот скрытого элемента | Pentest: анализ данных |

🛡️ **Особенности для безопасности**

| Объект | Уязвимость/Угроза | Последствие | Метод анализа |
|--------|----------------
